/**
 * 아군 부족 유닛 4종 — 마을에서 골드로 뽑아 판 위로 내보내는 전력.
 * 행동 규칙 전문은 src/sim/allies.ts 헤더 주석. 여기는 수치와 역할 분리만 적는다.
 *
 * ── 왜 3종이었나 ───────────────────────────────────────────────────────────
 * 적 습격대 4종이 "근접 둘 · 원거리 둘"로 갈리듯, 아군도 **막는 쪽 둘 · 쏘는 쪽 하나**로
 * 나눴다. 한 종만 두면 출동 버튼이 곧 "골드를 체력으로 바꾸는 레버"라 판단이 없고,
 * 다섯 종을 두면 웨이브 도중에 고를 수 있는 폭을 넘어선다(긴급 자원은 1탭이어야 한다).
 *
 * ── 넷째가 붙은 이유 (채집, docs/gather-spec.md) ────────────────────────────
 * `gatherer`는 위 셋과 **같은 축에 있지 않다.** 앞의 셋이 "막는가 쏘는가"로 갈리는 반면
 * 채집꾼은 **싸우지 않는 카드**다 — 2.25dps에 봉쇄 0이라 전투 표에서는 자리가 없고,
 * 이 카드가 파는 것은 화력이 아니라 **가격**이다. 실측이 잰 무일푼 구간(판의 24.62%,
 * w1-10은 55.29%)에서 최저 매물가가 언제나 정확히 90골드(몽둥이꾼)였는데, 70짜리가
 * 들어오는 순간 그 값이 90 → 70이 되고 부족액이 35~52 → 15~32로 준다.
 * ⚠ **이 한 걸음은 채집 골드가 0이어도 일어난다** — 그래서 넷째의 값은 cost 70 그 자체이고,
 *   짐값은 그 뒤에 오는 두 번째 층이다(gather-spec §0 · §8-2).
 * 그러므로 "다섯 종은 폭을 넘는다"는 위 문장은 여전히 참이다: 웨이브 도중 고르는
 * **전투 카드**는 지금도 셋이다.
 *
 * 역할:
 *  · clubber  = 기준점. 싸고 빠르고 잘 때린다. 대신 오래 못 버틴다.
 *  · guardian = 시간을 사는 카드. 피해는 clubber의 절반인데 hp는 2.9배 —
 *               "죽이려고" 내보내는 게 아니라 **한 놈을 오래 세워 두려고** 내보낸다.
 *               단계 3부터 **자기가 붙잡은 적의 가죽을 연다**(sunder) — 아군이
 *               타워 화력의 곱셈 인자가 되는 유일한 자리다.
 *  · slinger  = 유일하게 공중을 치고, 유일하게 아무도 막지 못한다.
 *               막지 못하는 대신 앞의 근접 아군 뒤에서 안전하게 딜을 넣는다
 *               (적은 **자기를 막은 아군만** 반격한다 — allies.ts 규칙 5).
 *  · gatherer = 싸우지 않는 카드. 캐는 손이 전투 3종의 3배이고(gatherPct 300) 짐을 둘 진다
 *               (carryCap 2). 실측 왕복 비교로 전투 3종보다 **2.4~2.9배**(중앙값 2.6배) 번다.
 *               넷 중 가장 싸고(70) 가장 빠르며(1.30) 가장 약하다.
 *
 * ── 수치의 기준선 ──────────────────────────────────────────────────────────
 * 타워 T1 배치가 90~150골드다. clubber 40은 그 절반 이하 — 되돌아오지 않는 20초짜리이므로
 * 영구 구조물보다 싸야 한다. 6명을 다 채우면 398골드(ALLY_COST_GROWTH 1.20)로
 * 스테이지1 시작 골드의 1.3배 — "이번 웨이브를 넘길 것인가 다음을 준비할 것인가"가
 * 실제 선택이 되는 크기다.
 *
 * ⚠ 9단계에 **영구 유닛이 됐다** (사용자 재정의). 수명 600틱(20초)이 사라지고 HP만 남았다.
 * "쌓아 두는" 플레이를 막던 일은 이제 정원(BaseLevelDef.allyCap, Lv1 2명)이 한다.
 * 아래 4단계 튜닝 기록의 가격(40/60/85)은 **소모품 시절의 값**이라 그대로 두면 안 됐다 —
 * 세 종 모두 영구 유닛 값대로 올렸다(90/110/160). 성능 수치는 여전히 하나도 안 건드렸다.
 *
 * ── 4단계 튜닝 (실측 기반) ─────────────────────────────────────────────────
 * 기본가를 27% 내렸다: 몽둥이꾼 55→40 · 무릿매 80→60 · 파수꾼 110→85.
 * 성능(hp/dmg/사거리/수명)은 **하나도 건드리지 않았다** — 무한 모드 A/B에서
 * dmg×2 · hp×2 · 사거리×1.6 · 수명×3을 각각 돌려도 도달 웨이브가 전부 노이즈 안이었다.
 * 아군의 병목은 세기가 아니라 **가동률**(이기는 판 1.1% · 지는 판 3.5% — 5단계 정정값,
 * 근거는 balance.ALLY_RETIRE_REFUND 주석)이라, 성능을 올리면
 * "안 쓰이는 시간이 더 비싸질" 뿐이고 값을 내려야 수지가 맞는다.
 * 나머지 두 손잡이는 balance.ts에 있다: ALLY_BLOCK_CAPACITY(정원 봉쇄)와
 * ALLY_RETIRE_REFUND(귀환 환급). 근거는 각 상수 주석.
 *
 * ══ ⚠⚠ 봉투 [11-b] 재유도 — **이 파일의 값은 한 자리도 안 움직였다** ══════════
 * 지시는 "src/data/allies.ts 를 재유도하라"였고, 후보로 정원 곡선 · ALLY_BLOCK_CAPACITY ·
 * 부족원 사거리 · 아군 비용이 지목됐다. **넷 다 스윕으로 재고 전부 기각했다.**
 * 병목이 아군의 값이 아니라 **집결 대열의 기하**였기 때문이다. 실측을 그대로 적는다
 * (스테이지1 · `{towerReserve: 600, allies:{minNear:1}}` · 창 `cap` 2블록 × 40 = 80판.
 *  문턱은 생산 1.10 · 봉쇄 1.25).
 *
 *   ① 손잡이를 돌려 본 결과 — **아무것도 안 닫힌다**
 *      ALLY_BLOCK_CAPACITY  3 → 봉쇄비 0.976 · 2 → 1.011 · 1 → **0.874**(더 나빠진다)
 *      부족원 사거리 ×1.5 → 1.079 · ×2.0 → 1.041 · ×2.8 → 1.004 · ×4.0 → 1.172
 *      (사거리는 **생산비**만 1.05 → 1.22 로 닫는다. 봉쇄비는 네 배를 줘도 문턱 아래다)
 *
 *   ② 왜 안 닫히나 — 지표가 세는 것을 그대로 따라가면 보인다.
 *      `allyBlockTicks` 는 그 틱에 **적을 붙잡고 있는 아군의 머릿수**다. 곧 정원 6이
 *      정원 2를 이기려면 **여섯째 사람이 첫째 사람이 못 잡는 적을 잡아야** 한다.
 *      그런데 여섯이 전부 한 점(집결 대열은 1.2 × 0.6타일이다)에 서 있으면 여섯이 **같은
 *      적을 본다**. 한 명이 `ALLY_BLOCK_CAPACITY` 3 마리까지 덮으므로, 마을 앞에 동시에
 *      서 있는 적이 셋을 넘지 않는 한 나머지 다섯은 구조적으로 놀고 있다
 *      (실측 가동률: 정원 2 에서 1.23% → 정원 6 에서 **0.40%**. 인원은 세 배인데 붙잡는
 *       총량이 그대로다). 그래서 값을 어느 쪽으로 돌려도 이 비가 1 근처를 못 벗어난다.
 *
 *   ③ 그리고 §C(문간 정지선 1.15 → 1.45)가 그 상태를 **더 나쁘게** 만들었다.
 *      문간선이 1.67~2.25 로 나가자 옛 대열의 뒷줄(마을 앞 **0.8**)은 근접 사거리
 *      1.0~1.15 로는 그 선에 아예 안 닿는다. 실측이 그대로 무너졌다:
 *      ⚠ 아래 두 줄은 **그때 잰 값**이다. 그 뒤 §C² 에서 정지선 잣대가 `radius` →
 *        `restReach` 로 바뀌어 문간선이 다시 **1.85~2.99** 로 나갔다 — 곧 ③의 진단은
 *        더 강해졌고 ④의 처방(대열이 앞으로 깊어진다)이 그만큼 더 필요해졌다.
 *        배포본 대열 · §C 전 → 생산비 1.048 · 봉쇄비 0.976
 *        배포본 대열 · §C 후 → 생산비 **0.857** · 봉쇄비 **0.580** (가동률 1.12% → 0.22%)
 *
 *   ④ **고친 곳 = 대열이 깊어지는 방향.** `forward = 1.4 − row × 0.6`(뒤로) 을
 *      `forward = 1.4 + row × ALLY_MUSTER_ROW_GAP`(앞으로) 로 바꿔 두 줄이 문간선을
 *      앞뒤로 감싸게 했다. 아군의 능력치·가격·정원 곡선은 **한 자리도 안 건드렸다.**
 *        같은 80판 → 생산비 **1.403** · 봉쇄비 **1.404** (가동률 1.12% → 0.60%)
 *        봉투가 실제로 쓰는 160판(FULL) → 생산비 **1.3716** · 봉쇄비 **1.4696**
 *        (문턱 1.10 / 1.25 — 여유 0.27 / 0.22)
 *      유도 전문은 balance.ts `ALLY_MUSTER_ROW_GAP` 주석.
 *
 *   ⑤ 왜 값이 아니라 기하가 옳은 처방인가: 값을 올리는 처방은 아군을 **일방적으로**
 *      세게 만들어 [7][8][10][12][13] 을 전부 위험한 쪽으로 민다(이 파일 규칙 8-④가
 *      이미 적어 둔 부채다). 대열을 옮기는 것은 **같은 여섯 명을 닿는 자리에 세우는 것**
 *      이라 아군의 총 전투력이 안 오른다. 곧 [11-b] 가 원래 재려던 것("마을이 판
 *      머릿수가 실제로 배달되는가")을 정확히 되돌려 놓는다.
 *
 * ── 골드당 체력 서열 — 이 표를 읽는 법 (단계 7) ────────────────────────────
 *                   hp/cost      랩터 난투(6) 앞의 버팀 횟수/골드
 *   몽둥이꾼 240/40 = 6.00        40타 / 40골드 = 1.00
 *   무릿매    80/60 = 1.33        — (아래 참조: **이 칸은 재는 것이 없다**)
 *   파수꾼   560/85 = 6.59        187타 / 85골드 = **2.20**  ← 몽둥이꾼의 2.2배
 *
 * **hp/cost 만 보면 서열이 거의 무너진 것처럼 보인다**(파수꾼이 몽둥이꾼의 1.10배뿐).
 * 그러나 이 카드가 파는 것은 체력이 아니라 **버티는 시간**이고, 그 계산에는 armor 3이
 * 들어간다 — 잡졸 난투(6)에서 파수꾼은 한 대에 3만 잃으므로 실효 체력이 두 배다.
 * 곧 **"시간을 사는 카드가 골드당 시간을 더 산다"는 단계 3의 명제는 그대로 참**이고
 * (2.20배), 큰 놈(trike 난투 25) 앞에서도 1.25배로 남는다. 다만 단계 3 시점의
 * 2.69배·1.53배보다는 좁아졌다 — 몽둥이꾼이 정원 3에 맞춰 올라온 만큼이다.
 *
 * **무릿매의 1.33은 낮은 게 아니라 값이 붙지 않은 칸이다.** 이 종은 `blocks: false`라
 * 절대 봉쇄자가 되지 않고, 적의 난투 반격은 `blockerAllyId`가 가리키는 아군에게만
 * 간다(sim/allies.ts 규칙 5 · sim/combat.ts damageAlly의 유일한 호출부). 곧
 * **무릿매는 시뮬레이션에서 단 한 번도 피해를 받지 않는다.** 단계 7에서 실측으로
 * 확인했다 — hp를 80 → 300으로 올리고 봉투 16번 국면을 80시드 돌리면 승수·잔여 HP·
 * 봉쇄 인원틱·생산 수가 **전부 비트 단위로 같다**. 그래서 이 칸은 밸런스 손잡이가
 * 아니라 **표시용 숫자**이고, 80은 "유리대포"라는 읽는 값으로만 뜻이 있다.
 * (앞의 근접 아군이 전부 쓰러지면 무릿매도 위험해 보이지만, 그때도 무릿매를 막는
 *  적은 무릿매를 때리지 않고 그냥 지나간다. 원거리 적이 아군을 조준하는 규칙이
 *  생기는 날 이 칸에 처음으로 값이 붙는다 — 그때 다시 유도해야 한다)
 */
import type { AllyDef, AllyId } from './types';

export const ALLY_DEFS: Record<AllyId, AllyDef> = {
  clubber: {
    id: 'clubber',
    nameKey: 'ally.clubber.name',
    descKey: 'ally.clubber.desc',
    // ── 단계 7: 140 → 240 (봉투 16번을 **독립 4벌 전부**에서 참으로 만든 투여량) ──
    // 140은 "랩터 한 마리 앞에서 23초 = 수명과 거의 같다"에 맞춘 값이었다. 그런데
    // 정원 봉쇄가 들어온 뒤로 실전의 기준 상황은 **한 마리가 아니라 여러 마리**다 —
    // 봉쇄 정원 3 기준으로 140은 18dps를 맞아 7.8초에 쓰러졌고(수명의 39%) 240이면
    // 13.3초(67%)였다. 곧 이 수치는 세기를 더 준 것이 아니라 **봉쇄 정원이라는 규칙에
    // 맞춰 기준선을 다시 잡은 것**이다(단계 3이 파수꾼에게 한 것과 같은 교정).
    // ⚠ 이 라운드에 `ALLY_BLOCK_CAPACITY` 가 **3 → 2** 로 갔다(유도는 그 상수 주석).
    //   위 계산의 기준 상황이 12dps · 20.0초로 바뀌지만 **hp 는 안 건드렸다**: 이 값이
    //   사던 것은 절대 초 수가 아니라 파수꾼과의 격차이고(240/12 = 20초 대 560/6 = 93초),
    //   그 격차는 정원이 줄면 오히려 **벌어진다**. 봉투 [8] 의 두 다리(`8.perHead` 42.77 ·
    //   `8.real.dominates` 15:1 p 2.59e-4)가 실측으로 그것을 확인한다.
    //
    // ⚠ 이 값만 올리면 **거꾸로 나빠진다.** 파수꾼 400을 그대로 두고 몽둥이꾼만 올린
    // 640시드 실측(수열 1001, 진짜/위약): 140 → 83/88 · 160 → 82/88 · 200 → 84/88 ·
    // 280 → 86/88 로 **한 번도 위약을 못 넘는다.** 몽둥이꾼 hp는 파수꾼 hp ≥ 520과
    // **함께여야만** 값을 한다 — 유도 전문은 docs/counter-plan.md 마무리 절.
    hp: 240,
    speed: 1.15,
    armor: 0,
    radius: 0.26,
    // 9단계: 40 → 90. 소모품(20초)에서 **영구 유닛**이 됐으니 값도 영구 유닛의 값이다.
    // 기준: 창움막 T1이 100골드에 260HP·24dps이고 몽둥이꾼은 90에 240HP·17.5dps —
    // 화력은 아래, 맷집은 비슷, 대신 **자리를 옮길 수 있다**. 그 하나가 값의 차이다.
    cost: 90,
    // 14 / 0.8초 = 17.5 dps. spear T1(12/0.5초 = 24dps)보다 낮게 둔다 —
    // 아군은 화력이 아니라 **위치**를 사는 카드여야 타워가 밀려나지 않는다.
    dmg: 14,
    cooldownTicks: 24,
    range: 1.0,
    canTargetAir: false,
    blocks: true,
    // **기준 = "한 사람 몫"**. 100이 곧 RESOURCE_DEFS.ticks 표의 분모다(딸기 12.0초 · 돌 33.0초).
    // 0으로 두면 안 되는 이유는 정원이다: Lv1 정원이 2라(hometown.ts BASE_LEVELS)
    // 전투 종이 아예 못 캐면 웨이브 1의 선택지가 "채집꾼2 = 방어0" 또는 "전투2 = 채집0"
    // 이분법뿐이고, 실제 정답은 늘 그 중간이라 **선택이 사라진다**. 100을 두면 정원 2에서
    // 세 조합이 전부 다른 답이 된다 — 그리고 "전투원도 딸기는 딸 만하다(12초),
    // 돌은 못 딴다(33초)"가 채집꾼을 사야 하는 이유를 설명이 아니라 초 단위 숫자로 만든다.
    gatherPct: 100,
    // 전투 3종은 전부 1. 짐 하나에 왕복 하나 — 이동 시간이 짐 하나에 통째로 걸린다.
    // (채집꾼만 2라서 먼 칸일수록 상대적으로 강해진다. gather-spec §2-3)
    carryCap: 1,
  },
  guardian: {
    id: 'guardian',
    nameKey: 'ally.guardian.name',
    descKey: 'ally.guardian.desc',
    // armor 3 + hp 400. 잡졸 난투(2~6)를 armor가 절반 이상 깎아 "떼에는 강하고
    // 큰 놈에게는 약하다"가 성립한다 — trike(25)에게는 armor가 12%밖에 안 된다.
    // 4단계 정원 봉쇄(ALLY_BLOCK_CAPACITY 3)가 이 armor를 비로소 역할로 만든다:
    // 랩터 3마리를 동시에 붙잡으면 몽둥이꾼은 18dps를 맞아 7.8초에 쓰러지지만
    // 파수꾼은 armor가 마리당 3을 깎아 9dps라 수명 20초를 그대로 채운다.
    //
    // ── 단계 3: 320 → 400 (봉투 16번을 참으로 만든 투여량) ────────────────────
    // 320은 **절벽 위에 서 있었다.** 봉투 16번 국면(마을 Lv3 고정 · 예비비 600 ·
    // minNear 1)을 80시드 블록 8벌 = 640시드로 재면 진짜 아군의 승수가
    //   hp 320 → **78** · 330 → 92 · 340 → 92 · 350 → 94 · 360 → 92 · 400 → **92** ·
    //   440 → 95 · 480 → 92   (위약은 전 구간 72로 불변)
    // 곧 330 이상은 전부 같은 고원이고 **커밋된 값 320만 그 고원 아래**였다.
    // 400은 그 고원의 한가운데다 — 절벽(≈325)에서 23% 위라 다른 축이 흔들려도
    // 다시 굴러떨어지지 않는다.
    //
    // 왜 값(85)이 아니라 성능을 움직였는가: **위약은 가격을 그대로 따라온다.**
    // 같은 640시드에서 값을 85 → 60으로 내리면 진짜 82 대 위약 80(+2)이지만,
    // hp를 올리면 위약이 한 자리도 안 움직여 진짜 92 대 위약 72(+20)다. 위약이
    // 지우는 것은 **전투 능력**이므로 격차를 벌 수 있는 것도 전투 능력뿐이다.
    // (근거·전 표는 docs/counter-plan.md 단계 3 착수 결과)
    //
    // 그리고 이 카드의 값이 원래 무엇이었는지도 이 수치가 고친다: 320/85 =
    // 골드당 3.76 체력으로 **몽둥이꾼(140/40 = 3.50)과 사실상 같았다.**
    // "시간을 사는 카드"가 골드당 시간을 더 안 사고 있었던 것이다.
    //
    // ── 단계 7: 400 → 560 (봉투 16번의 **궤적 의존**을 없앤 투여량) ─────────────
    // 단계 3의 400은 봉투가 어서션하는 80시드 블록에서는 초록이었지만, 시작점을 옮긴
    // 독립 640시드 수열 4벌로 재면 **2벌에서만 참**이었다(승수 92/72 · 83/88 ✗ ·
    // 96/86 · 85/87 ✗). 곧 그 초록은 부분적으로 궤적 운이었다.
    //
    // 같은 4벌 잣대로 파수꾼 hp만 훑으면 **절벽이 500 언저리에 있다**(수열 1001,
    // 진짜/위약 · 몽둥이꾼 140 고정):
    //   440 → 83/88 ✗ · 480 → 84/88 ✗ · **520 → 91/88** · 560 → 91/88 ·
    //   600 → 91/88 · 640 → 91/88     ← 520 이상이 전부 같은 고원
    // 몽둥이꾼을 280까지 올려도 파수꾼 480에서는 여전히 87/88로 진다. 곧
    // **파수꾼 ≥ 520은 다른 무엇으로도 대체되지 않는 필요조건**이다.
    // (armor로는 안 된다 — armor 3 → 5 · 7 · 몽둥이꾼 armor 2를 각각 재도 1001은
    //  86 · 86 · 82로 전부 위약 아래다. 이 국면이 사는 것은 **서 있는 시간**이지
    //  한 대당 감산이 아니다)
    // 560은 그 절벽에서 12% 위이고, 커밋된 블록의 여유가 이 영역에서 얻을 수 있는
    // 최댓값(+2판)이 되는 자리다. 640(단계 3이 대안으로 재 뒀던 값)은 같은 4벌을
    // 통과하지만 커밋 블록 여유가 +1판이라 CI가 먼저 빨개진다.
    hp: 560,
    armor: 3,
    speed: 0.85,
    radius: 0.3,
    // 9단계: 85 → 160 (영구화). 560HP·armor 3은 이 게임에서 가장 단단한 물건이고
    // sunder까지 얹혀 있다 — 영구가 된 순간 창움막 T2(200골드)와 같은 값대에 놓인다.
    cost: 160,
    // 9 / 1초 = 9 dps. clubber의 절반 — 이 카드는 죽이는 게 아니라 세워 두는 카드다.
    dmg: 9,
    cooldownTicks: 30,
    range: 1.15,
    canTargetAir: false,
    blocks: true,
    // 급소를 여는 손 — 이 카드가 붙잡은 적은 가죽 상한이 사라진다 (단계 3).
    // 파수꾼만 갖는 이유: 셋 중 유일하게 "오래 세워 두는" 카드이고(hp 400 · armor 3),
    // 상한이 열려 있는 **시간**이 곧 이 효과의 크기다. 몽둥이꾼(hp 140)에게 주면
    // 같은 85골드어치를 사는 데 두 명이 필요해 카드 구분이 사라진다.
    sunder: true,
    // 넷 중 가장 느린 손(딸기 20.0초 · 부싯돌 40.0초 · 돌 55.0초). speed 0.85에 방패까지
    // 들었으니 캐는 것도 느리다는 그림이 그대로 값이 된다. 이 60이 사는 것은 한 문장이다:
    // **"파수꾼을 채집에 돌리면 손해다."** 곧 채집이 "아무나 시키면 되는 잡일"이 아니라
    // 종을 고르는 판단이 된다 — 이 카드는 서 있는 시간이 값인데, 돌 하나에 55초를 쓰면
    // 그 값을 스스로 태운다.
    gatherPct: 60,
    carryCap: 1,
  },
  slinger: {
    id: 'slinger',
    nameKey: 'ally.slinger.name',
    descKey: 'ally.slinger.desc',
    // 유리대포 — 적 궁수(65)와 같은 자리. **표시용 숫자다**: 이 종은 blocks가 false라
    // 봉쇄자가 되지 않고, 난투 반격은 봉쇄자에게만 간다(규칙 5) → 실제로 피해를 받는
    // 경로가 없다. 단계 7에서 80 → 300 A/B가 비트 단위로 같음을 확인했다.
    // 근거와 "그럼 언제 값이 붙는가"는 이 파일 헤더의 골드당 체력 서열 절.
    hp: 80,
    speed: 1.05,
    armor: 0,
    radius: 0.24,
    // 9단계: 60 → 110 (영구화). 유일한 대공이고 사거리 2.8이라 값이 비싸야 한다.
    // ⚠ hp 80은 8단계까지 **읽히지 않는 값**이었다(blocks:false라 봉쇄자가 안 되고,
    // 난투 반격은 봉쇄자에게만 갔다). 영구화로 그게 구멍이 됐다 — 아무도 못 때리는
    // 영구 유닛은 불멸이다. 규칙 5-c(스치는 타격)가 그 구멍을 막으면서 이 80이
    // **처음으로 실제 밸런스 값이 됐다**: 무릿매는 전선에 세우면 금방 죽는다.
    cost: 110,
    // 11 / 1초 = 11 dps. 사거리 2.8은 근접 아군(1.0~1.15) 뒤에 서서 쏘기에 충분하고
    // 아군 출격 한계선(6.0) 안에서 길목 하나를 통째로 덮지는 못하는 폭이다.
    dmg: 11,
    cooldownTicks: 30,
    range: 2.8,
    canTargetAir: true,
    blocks: false,
    // clubber와 같은 기준값. 채집 속도에 원거리/근접 구분을 넣지 않는 이유는 이 축이
    // 재는 것이 **손의 빠르기**이지 무기가 아니기 때문이다 — 무기로 값을 가르기 시작하면
    // 표가 종별 4행으로 늘고 "왜 돌팔매가 벌집을 더 빨리 따는가"를 아무도 설명 못 한다.
    gatherPct: 100,
    carryCap: 1,
  },
  gatherer: {
    id: 'gatherer',
    nameKey: 'ally.gatherer.name',
    descKey: 'ally.gatherer.desc',
    // 몽둥이꾼 240의 **절반**. ⚠ 이 값이 재는 것은 사망이 아니다 — 경로에서 두 칸 이상
    // 떨어진 채집꾼은 한 대도 안 맞으므로 죽는 일 자체가 드물다. 실제로 물리는 대가는
    // **"언제 손이 멈추나"** 다: 맞으면 캐던 진행분이 0으로 돌아간다(gather-spec D5).
    // 실측 — 스테이지1 소품 40칸 중 **22칸(55%)이 BRAWL_BRUSH_RANGE 1.1 안**이라
    // (경로거리 중앙값 1.04) 지나가는 적이 스치기만 해도 손이 멈춘다. 곧 120은
    // "몇 대까지 버티나"이고, 게임이 실제로 물리는 벌금은 그 앞의 한 대다.
    hp: 120,
    // 넷 중 최고 (clubber 1.15 · slinger 1.05 · guardian 0.85). 판을 가로질러 칸을 도는 것이
    // 본업이라 **이동 시간이 곧 생산성**이다 — 중앙값 칸 wood 한 짐이 15.6초인데 그중
    // 걷는 시간이 6.6초다. 전투로 전용될 걱정이 없는 이유는 range 0.9 · dmg 3이다.
    speed: 1.3,
    // armor는 **파수꾼의 정체성**("떼에는 강하고 큰 놈에게는 약하다")이다. 침식 금지 —
    // 채집꾼에게 armor를 주면 "맞아도 안 멈추는 일꾼"이 되어 D5의 벌금이 통째로 사라진다.
    armor: 0,
    // 무릿매와 같은 값 = 넷 중 가장 작은 몸. "무장하지 않은 사람"이라는 그림 그대로다.
    radius: 0.24,
    // ⚠ **이 값이 이 기능에서 가장 큰 한 걸음이다** — 짐값이 0이어도 일어나는 몫이다.
    // 실측: 무일푼 구간(판의 24.62% · w1-10은 55.29%)에서 최저 매물가가 **언제나 정확히
    // 90골드**(몽둥이꾼)였고 잔고 중앙값은 38~55였다. 70이 들어오면 부족액이
    // 35~52 → **15~32**로 준다. 유도는 clubber 90의 78%.
    // ALLY_COST_GROWTH 1.05이므로 2명 실비용 = 70 + round(70×1.05) = **144골드**,
    // s1 시작 골드 300의 48% — 첫 타워(100~150)와 정면으로 다투는 크기다.
    // ⚠ guardian급 160은 기각: 160 + 168 = 328 > 300이라 Lv1 정원 2를 첫 판에 못 채운다
    //   = 기능이 도달 불가가 된다.
    // 하한은 코드에 있다: tests/data/validate.test.ts가 cost > 45를 잠근다(70 > 45 ✅).
    cost: 70,
    // **0을 쓰면 안 된다**: combat.ts의 Math.max(1, raw - armor)이라 dmg 0은 "공격 없음"이
    // 아니라 **매 타격 1피해 + 살점 값 지급**이다. 곧 0은 크래시가 아니라 화면에서
    // 읽히지 않는 값이 된다. 3이면 3 / 1.33초 = 2.25dps = clubber(17.5)의 **12.9%**다.
    dmg: 3,
    cooldownTicks: 40,
    // 넷 중 최단. **0으로 두면 안 된다** — 위약 아군(tests/sim/autoplay.probes.ts)이 쓰는
    // 값이고, 조준이 안 서서 공격 연출이 영영 안 나며, placement.ts의 사거리 원이
    // 반지름 0이 된다. 0.9는 clubber 1.0보다 짧아 "먼저 붙는 쪽이 절대 이 카드가 아니다".
    range: 0.9,
    // 대공은 **무릿매의 유일한 정체성**이다. 여기에 주면 그 카드가 지워진다.
    canTargetAir: false,
    // blocks:true면 "정원 6을 전부 채집꾼"이 612골드 대신 약 470골드로 같은 봉쇄 폭을 사서,
    // ALLY_BLOCK_CAPACITY 3에 맞춰 다시 잡은 clubber hp 240의 유도를 통째로 우회한다.
    // 부수효과 하나: moveAllies의 `if (a.def.blocks && a.targetId >= 0) continue`를 안 타므로
    // **걸으면서 때린다** — 무릿매와 같은 규약이고 새 규칙이 아니다.
    blocks: false,
    // ── 사용자 요구 (2)의 실체 — "일꾼이 더 많이 채집한다"의 크기 ────────────────
    // 200이면 "채집 2배 대 전투 전부"라 70골드가 90골드를 못 이긴다(전투력 차가 100%인데
    // 이득은 100%뿐). 500이면 정원 2에서 "채집꾼1 + 전투1"이 유일한 답이 되어 선택이 사라진다.
    // 300에서 실측 왕복 비교가 **전투 3종 대비 2.4~2.9배**(중앙값 2.6배)로 앉는다.
    // 2.6배가 셋의 곱이 아니라 합인 이유: 짐당 시간이 "걷기 + 캐기"의 **합**이라
    // 속도 배수를 키워도 걷는 시간은 안 줄어든다 — 그래서 이 축은 500으로 올려도
    // 중앙값 칸에서 3.4배로 폭주하지 않는다.
    // ⚠⚠ **속도에만 곱한다. 수확량에는 절대 안 곱한다**(D8) — 칸의 짐값이 셀에 고정돼야
    //   맵 총액이 Σ(칸별 짐값) = s1 559골드로 닫히고 감사 가능한 한 숫자가 된다.
    gatherPct: 300,
    // ⚠ **수확 배율이 아니다.** 총액은 그대로 닫히고 바뀌는 것은 **왕복 횟수**뿐이다.
    // 왜 2인가: 실측으로 스테이지1 소품 40칸의 **최근접 이웃 거리 중앙값이 정확히 1.00타일**
    // (최소 1.00 · 최대 3.16)이라 두 번째 칸은 사실상 **옆칸**이다. 곧 이 값이 사는 것은
    // "두 배로 캔다"가 아니라 **"마을 왕복 하나를 아낀다"** 이고 그 크기가 거리에 비례해
    // 커진다(짐당 시간: 가까운 칸 ×1.07 · 중앙값 칸 ×1.37 · 먼 칸 ×1.52).
    // 그리고 이 값이 §0 논지의 구성요소다 — 앞칸 한 짐(9골드)은 부족액(15~32)을 못 덮지만
    // **두 짐(17~20)은 그 아래 절반을 한 번에 덮는다.**
    // **3은 안 쓴다** — 이득은 더 있지만(중앙값 칸 +14%) 셋이 막는다:
    //  ① 먼 칸 세 짐이면 **50.6초 연속** 전투 불능 = 웨이브 한 개 반(웨이브 17.3~36.9초).
    //     15.6초짜리 결정과 50초짜리 결정은 같은 종류의 결정이 아니다.
    //  ② 한 번 죽을 때 잃는 액수가 최대 66골드 = 총액 559의 **11.8%**(cap 2는 46 = 8.2%).
    //     되돌릴 수 없는 손실의 분산이 그만큼 커지면 "먼 칸에 가지 마라"가 정답이 된다.
    //  ③ 자동 이웃 이동이 없으므로(D4) 3짐을 채우려면 플레이어가 세 번 개입해야 하고,
    //     그 사이 **두 번을 서 있게 된다** — 그 대기가 그대로 방치 유혹이다(계약 A).
    carryCap: 2,
  },
};

/**
 * UI/렌더 순회용 고정 순서 (출동 바 버튼 순서이기도 하다 — **싼 것부터**).
 *
 * ⚠ 채집꾼이 70골드라 **맨 앞으로 온다** — 곧 출동 바의 첫 버튼이 clubber에서 gatherer로
 *   바뀐다. 그것이 이 배열의 규약("싼 것부터")이 뜻하는 그대로이고, 동시에 이 기능이
 *   실제로 파는 것과도 맞는다: 무일푼 구간에서 처음 손이 닿는 매물이 이제 이 카드다.
 *   순서를 지키려고 예외를 두면 "싼 것부터"라는 한 줄 규약이 그 순간 거짓이 된다.
 *
 * ⚠ 이 배열은 **tsc가 못 잡는 자리**다(길이도 순서도 타입이 아니다). 여기에 종을 더하면
 *   같은 순서를 전제하는 곳들(렌더 kit 표·공격 표·HUD 버튼 폭)이 조용히 어긋나므로
 *   각 트랙이 직접 세어야 한다.
 */
export const ALL_ALLY_IDS: readonly AllyId[] = ['gatherer', 'clubber', 'slinger', 'guardian'];
