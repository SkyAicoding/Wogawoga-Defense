/**
 * 적 16종 정의 — hp는 스테이지1 웨이브1 기준(웨이브젠이 hpMul 적용).
 * bounty = round(cost × 0.8) 규칙 고정 (tests/data가 잠근다).
 * cost는 웨이브젠 예산 소비 단위이자 전투력 지표.
 *
 * ── 부족 습격대 4종 (blade/lancer/archer/hexer) ────────────────────────────
 * 공룡·짐승과 달리 **기지가 아니라 우리 타워를 노리는 무리**다. 역할 분리:
 *  · warrior(기존) = 방패로 버티며 기지로 직행하는 탱커, shaman(기존) = 힐러.
 *    둘은 "웨이브를 끝까지 살아서 통과시키는" 쪽이고,
 *  · 습격대 4종은 "지나가며 방어선을 허무는" 쪽이다 — 그래서 hp/cost 효율이
 *    일부러 나쁘다(2.5~4.8, 공룡은 5~9.3). 이들의 전투력은 체력이 아니라
 *    타워에 넣는 피해에 들어 있고, cost가 그만큼을 대신 지불한다.
 *
 * ── 사거리·정지 규약 (siege.ts 규칙 4와 짝) ────────────────────────────────
 * **습격대 4종 + warrior는 전부 원거리이고, 전부 공격 가능 지점에서 멈춰 선다.**
 * 근접(옛 blade 1.5 / lancer 1.95)을 버린 이유는 밸런스가 아니라 기능 부재다 —
 * 아래 archer 주석의 실측("칼잡이 108명의 타워 피해 총합 0")이 근거다.
 *
 * 네 축으로 종을 가른다. 어느 하나도 두 종이 같지 않다:
 *   사거리   blade 2.4 < lancer 2.8 < archer 3.2 < hexer 3.6
 *   버티는 시간 lancer 90 > blade·archer 75 > hexer 60   (holdTicks)
 *   한 방 대 연사 lancer 5/36틱(무겁게 드물게) ↔ blade 3/20틱(가볍게 자주)
 *   특수      blade 발 빠름 1.20 / lancer 장갑 3 / archer 유리몸 65 / hexer 침묵
 * (12단계에서 lancer 단발이 6 → 5가 되어 archer와 값이 같아졌다. 그 축을 가르는 것은
 *  이제 단발이 아니라 간격이다 — lancer 36틱 대 archer 40틱. 근거는 lancer 주석)
 *
 * 정지 거리는 종별 취향이 아니라 판의 기하가 정하는 공통값 SIEGE_ENGAGE_RANGE(1.7)이고
 * 거기에 towerReach 상한이 걸린다(규칙 4-a) — 그래서 사거리를 늘려도 "타워 사거리 밖에서
 * 일방적으로 두들기기"는 불가능하다. 사거리가 사는 것은 **정지 지점에 도달하기 전에
 * 걸으며 쏘는 구간의 길이**와 **닿는 타워의 범위**뿐이다.
 *
 * 한 방 피해가 옛 값의 1/3 수준으로 내려간 이유: 옛 수치는 "스쳐 지나가며 한두 대"를
 * 전제로 매겨졌는데, 이제는 정지 구간에서 쿨다운마다 꼬박꼬박 넣는다.
 * (blade 8→2 · lancer 15→5 · archer 11→4 · hexer 6→2 · warrior 10→3)
 *
 * ── 10단계(밸런스): blade 2→3 · lancer 5→6 · archer 4→5 ────────────────────
 * 9단계의 위 값들은 "정지선 2.1 = 이격 2칸 타워도 붙잡힌다"는 세계에서 매겨졌다.
 * 정지선을 1.7로 내려 **이격 2칸이 다시 안전해지자**(balance.SIEGE_ENGAGE_RANGE 주석)
 * 잘 둔 방어선이 받는 피해가 절반 아래로 떨어졌고(파괴 211→116기), 그만큼을
 * 되돌려 받은 것이 이 세 값이다. 셋 다 **거리 축을 되살린 뒤에** 잰 값이라
 * 9단계가 "어느 배율에서도 밀착 봇이 안 무너진다"고 적은 벽에 걸리지 않는다.
 * 종별 근거는 각 towerAttack 주석에 있다. hexer 2 / warrior 3은 그대로다 —
 * hexer는 침묵이 본체이고(직접 피해는 전 종 최저라는 자리 유지), warrior는
 * 습격대가 아니라 탱커라 습격대 하단(hexer 1.0 ~ lancer 5.0) 안에 있으면 된다.
 */
import type { EnemyDef, EnemyId } from './types';

/** bounty 규칙 상수 — 웨이브젠 예산 대비 골드 환류율 */
export const BOUNTY_PER_COST = 0.8;

export const ENEMY_DEFS: Record<EnemyId, EnemyDef> = {
  raptor: {
    id: 'raptor',
    nameKey: 'enemy.raptor',
    hp: 60,
    speed: 1.6,
    armor: 0,
    flying: false,
    bounty: 8,
    baseDamage: 1,
    radius: 0.3,
    /**
     * 흩어짐 〽 — 폭발 부가 피해 ×0.70. 무리로 몰려오는 종이라 "뭉친 것을 한 번에"가
     * 가장 잘 듣던 자리다.
     *
     * **0.30의 유도 (Phase 2 이분 탐색 실측, docs/counter-plan.md §단계 2):**
     * 이 축은 초심자(하한 팔)와 투석기 몰빵을 **같은 방향으로** 때리는데, 몰빵은 여유가
     * 26%나 있고 초심자는 0이라 **초심자가 먼저 죽는다**. 실측 프론티어 — 이 축 **단독**
     * (가죽은 끈 상태. 하한 팔 100시드 완주율 / 커밋된 20시드 블록 / {catapult:8} 승수·여유):
     *   0.30 → 68.0% / 14 / (측정 안 함 — 0.55까지 40:40이라 의미 없음)
     *   0.50 → 65.0% / 13 / 40:40 26.3%     0.58 → 55.0% / 12 / 40:40 23.0%
     *   0.60 → 51.0% / 10 / 38:40 19.3%     0.64 → 53.0% / 10 / 28:40 10.6%
     *   0.66 → 45.0% / 10 / 20:40  7.2%     0.68 →  —    / —  /  8:40  2.2%
     * 곧 **몰빵 여유가 처음 움직이는 0.60에서 하한 팔은 이미 10/20**이라 봉투 ④가 깨진다.
     * ①에 닿는 용량과 ④를 지키는 용량이 겹치지 않는다 — 그 사이를 메우는 것이
     * Phase 4(장갑)·Phase 6(사다리)의 몫이고, 여기서는 **온보딩에 값을 치르지 않는
     * 최대치**를 쓴다. 200시드(20시드 블록 10벌) 확인: 기준선 68.0% 대 0.30 **67.5%**
     * (블록별 차 합계 −1판/200), 0.35는 64.0%(−8판/200)라 여기서 끊었다.
     */
    splashResist: 0.3,
    cost: 10,
  },
  compy: {
    id: 'compy',
    nameKey: 'enemy.compy',
    hp: 25,
    speed: 1.9,
    armor: 0,
    flying: false,
    bounty: 4,
    baseDamage: 1,
    radius: 0.22,
    cost: 5,
  },
  trike: {
    id: 'trike',
    nameKey: 'enemy.trike',
    hp: 420,
    speed: 0.7,
    /**
     * 장갑 🛡 — 타격당 고정 감산(최소 1). **작은 타격**을 벌한다.
     *
     * ══ Phase 4: `4 → 6`을 검토하고 **올리지 않기로 했다** (docs/counter-plan.md 단계 4) ══
     * 계획서는 봉투 18 ②(`{spear:8}` 여유 ≤5%)를 만들 기전으로 이 값을 6으로 올리자고
     * 적었다. 세 가지가 실측으로 반박했다.
     *
     * **(1) 천장은 있다 — 이 축에는 질량이 충분하다.** 투여량 전에 극단값부터 쟀다
     * (`{spear:8}` 40시드 · 여유):
     *   armor 4 **13.4%** · 8 8.8% · 12 4.8% · 14 1.3% · **20 0.0%(0/40)**
     * 곧 ②는 **armor 12**에서 실제로 선다 — 시작점을 옮긴 독립 40시드 4벌에서
     * 여유 4.8 / 3.4 / 6.2 / 4.7% · 승수 8·8·15·9 (문턱 ≤24/40).
     * (Phase 3의 가죽 축은 천장 자체가 +7판이었다. 이 축은 그것과 다르다)
     *
     * **(2) 그런데 초심자가 먼저, 그리고 훨씬 더 많이 낸다.** 하한 팔 200시드 완주율
     * (20시드 블록 10벌 · base 1000. 기준선 = 축을 하나도 안 넣은 상태 68.0%):
     *   armor 4(현재) **67.5%** · 6 64.0% · 7 64.0% · 8 58.0% · 10 53.0% · **12 41.5%**
     * ②가 서는 armor 12에서 온보딩은 **68.0% → 41.5%**이고 커밋 블록은 **9/20**이다.
     * 봉투 ④(≥13/20)가 깨지고, 판정선 B의 예산(−2.0%p)은 −26.5%p로 **13배** 초과된다.
     * 교환비를 한 줄로: **②까지 남은 여유 8.8%p를 사는 값이 온보딩 26.5%p**다.
     *
     * **(3) 계획서 초안값 6이 사는 것은 정확히 0이다.** 독립 40시드 4벌 `{spear:8}` 여유
     *   13.4→14.4 · 10.4→10.6 · 15.9→15.1 · 15.3→16.1  (평균 13.75 → **14.05**)
     * 곧 6은 ②를 한 자리도 안 움직이면서 온보딩만 낸다 = **엄격히 지배당하는 눈금**이다.
     * (600시드로 재도 마찬가지다 — 아래 ⚠)
     *
     * ── 왜 그런가: armor는 "덱"이 아니라 **타격 크기**를 잰다 ────────────────────
     * 고정 감산이라 손해율이 타격 크기에 **반비례**한다. 같은 `+2`가:
     *   얼음 T1(7)   0.43 → **0.14**  (−67%. 감산 뒤 바닥값 1에 닿는다)
     *   창   T1(12)  0.67 → 0.50   (−25%)
     *   창   T3(28)  0.86 → 0.79   (−8%)
     *   투석기 T3(76) 0.95 → 0.92   (−3%)
     *   파수꾼(9)     dmg 5 → **3** (armor 8이면 **1**)
     * 곧 이 축이 제일 세게 무는 것은 창 몰빵이 아니라 **얼음과 아군**이다.
     * 기준선 봇은 손패의 1/3이 얼음이라 정면으로 받고, `{spear:8}`은 얼음을 한 기도
     * 안 짓는다. **계획서가 armor에 기대한 방향의 정반대**이고, Phase 2가
     * "가죽은 투석기가 아니라 창을 문다"를 발견한 것과 **같은 형태의 오진**이었다.
     * ⚠ 티어 집중도 가설도 세워 재 봤지만 **아니었다** — 20시드에서 타워 1기당 투입
     *   골드가 기준선 816 · 몰빵창 786 · 혼합 880으로 갈리지 않는다. 가르는 것은
     *   티어가 아니라 **덱에 얼음이 있는가**와 **그 팔이 한계 국면에 있는가**다.
     *
     * 그리고 몰빵 팔이 armor 10까지 거의 안 움직이다 12에서 무너지는 것은 **계단**이다.
     * trike가 실제로 서는 웨이브는 9개뿐이고(w20·28·30·32·33·37·39·**44·45**) 판을
     * 정하는 것은 마릿수가 몰린 w44(14마리)·w45(15마리)다. baseDamage가 2라 그 둘을
     * 막는 동안은 누수가 판을 못 바꾸고, 못 막는 순간 14×2 = 28 > 기지 25로 즉사한다.
     * 종료 HP 분포가 그대로 보여준다 — armor 4 `13×5 12×2 8×1 4×1 3×11 0×20` →
     * armor 8 `8×4 7×2 4×1 3×12 2×1 0×20`: **승수는 20 그대로이고 상위 꼬리만 깎인다.**
     *
     * ⚠⚠ **판정선 B의 예산은 이미 거의 다 썼다** (Phase 4에서 처음 600시드로 쟀다).
     *   독립 200시드 3벌(base 1000·1001·1002) 합산 완주율:
     *     축 없음 411/600 **68.5%**  ·  커밋된 현재 상태 400/600 **66.67%** (= −1.83%p)
     *     armor 6 392/600 65.33% (−3.17%p ✗)  ·  armor 8 366/600 61.00% (−7.50%p ✗)
     *   곧 지금 커밋된 수치가 이미 예산 −2.0%p의 **92%**를 쓴 상태다.
     *   (Phase 2가 −0.5%p로 기록한 것은 base 1000 한 벌만 본 값이다 — 한 벌로는
     *    이 항목을 판정할 수 없다는 그 파일의 경고가 자기 자신에게도 적용된다)
     *   Phase 5·6이 스테이지1 난이도를 더 올리려면 **먼저 초심자 쪽에 여유를 만들어야
     *   한다**. 계획서가 지목한 `catapult.toughness 1.25 → 1.05`가 그 자리다 —
     *   타워 체력이라 창·얼음에는 값이 0이고 투석기 8기 몰빵에서만 8배로 걸린다.
     */
    armor: 4,
    flying: false,
    bounty: 36,
    baseDamage: 2,
    radius: 0.52,
    cost: 45,
  },
  ptera: {
    id: 'ptera',
    nameKey: 'enemy.ptera',
    hp: 90,
    speed: 1.4,
    armor: 0,
    flying: true,
    bounty: 14,
    baseDamage: 1,
    radius: 0.32,
    cost: 18,
  },
  ankylo: {
    id: 'ankylo',
    nameKey: 'enemy.ankylo',
    hp: 300,
    speed: 0.6,
    armor: 10,
    flying: false,
    bounty: 32,
    baseDamage: 2,
    radius: 0.48,
    cost: 40,
  },
  boar: {
    id: 'boar',
    nameKey: 'enemy.boar',
    hp: 150,
    speed: 1.1,
    armor: 0,
    flying: false,
    bounty: 18,
    baseDamage: 1,
    radius: 0.38,
    // 저체력 격노 — 40% 이하에서 1.8배 가속
    enrage: { hpPct: 0.4, speedMul: 1.8 },
    /**
     * 가죽 🟫 — 타격당 상한 = round(maxHp × 0.18), 곧 **최소 6대**가 필요하다.
     * 티어·웨이브와 무관한 상수라 업그레이드로 빠져나갈 수 없다.
     *
     * **0.18의 유도 (Phase 2 이분 탐색 실측):** 하한 팔 100시드 완주율은
     * 0.20 → 68.0%(기준선과 동일) · **0.18 → 67.0%** · 0.16 → 66.0% · 0.14 → 65.0% ·
     * 0.08 → 60.0%. 곧 0.18은 온보딩에 사실상 값을 치르지 않는 최대치다.
     * 흩어짐 0.30과 함께 200시드로 확인한 최종 조합이 **67.5%**(기준선 68.0%)다.
     * 계획서 초안의 0.14를 쓰지 않은 이유는 그 −3%p가 **아무것도 사지 못하기** 때문이다:
     * {catapult:8} 여유는 hide 단독 0.14/0.11/0.08에서 25.7/26.0/26.2%로 **전혀 안 움직인다**
     * (투석기 화력의 대부분이 직격이 아니라 스플래시라 대상별 cap이 거의 안 문다).
     * 이 축이 실제로 무는 것은 **창 몰빵**이다 — {spear:8} 22/40 → 19/40.
     */
    hide: 0.18,
    cost: 22,
  },
  warrior: {
    id: 'warrior',
    nameKey: 'enemy.warrior',
    hp: 120,
    speed: 1.0,
    armor: 0,
    // 방패 — 첫 3회 피격 무시
    shieldHits: 3,
    flying: false,
    bounty: 24,
    baseDamage: 1,
    radius: 0.34,
    /**
     * 타워 공격 — **습격대는 아니지만 타워를 때리는 종**이라 개편 대상에 들어간다.
     * (사용자의 "남은 애들도 모두"를 *타워를 때리는 적 전부*로 읽었다. 근거는
     *  siege.ts 규칙 1 — 타워를 때리지 않는 공룡·짐승 11종에게 towerAttack을 새로
     *  주는 것은 요청에 없는 새 메커니즘이고, '경로 옆 타워만 위험'을 통째로 깬다)
     *
     * 다만 **습격대 등급은 주지 않는다**. warrior의 정체성은 방패로 버티며 기지로
     * 직행하는 탱커라, 사거리·정지 시간을 습격대만큼 주면 전선에서 멈춰 서느라
     * 기지에 못 간다. 사거리 2.2는 전 종 최단이고(습격대 하단 blade 2.4보다 짧다)
     * holdTicks 45도 전 종 최단이다.
     * (9단계에는 이 값이 'SIEGE_ENGAGE_RANGE(2.1)를 겨우 넘긴 최소치'라고 적혀 있었다.
     *  정지선이 1.7이 된 지금은 2.2가 그 최소치가 아니지만, 값을 바꿀 이유도 없다 —
     *  사거리 서열의 맨 아래라는 자리가 이 종이 사는 것이기 때문이다)
     *
     * ── 12단계: **"규칙 4를 물되 가장 얕게 문다"는 틀린 문장이었다** ────────────
     * 실측(tests/sim/arena.ts · 창던지기 4기 · warrior 12명 · 이격별):
     *   이격 1칸 → 타워 피해 639 · **정지 사격 21.9%**
     *   이격 2칸 → 타워 피해 261 · **정지 사격 0.0%**   ← 안전 이격
     *   이격 3칸 → 타워 피해 0    · 정지 0.0%
     * 곧 안전 이격(2칸) 앞에서 warrior는 **한 틱도 멈춰 서지 않는다.** 규칙 4를 얕게
     * 무는 게 아니라 **전혀 물지 않는다.** 기전은 정지선이지 사거리가 아니다 —
     * 정지 거리는 min(사거리, SIEGE_ENGAGE_RANGE 1.7, towerReach)라 2칸(2.0) 밖의
     * 타워 앞에서는 어떤 종도 설 수 없다(siege.ts 규칙 4-a). 사거리 2.2가 사는 것은
     * "지나가며 쏘는 구간"뿐이다.
     *
     * 그리고 그 구간이 실제로 얼마인지도 남긴다: 이격 2에서 사거리 2.2가 경로를 덮는
     * 길이는 2·√(2.2²−2²) = 1.83타일, 속도 1.0이면 1.83초 = 55틱이라 쿨다운 30틱으로
     * **통과당 1~2발(약 6피해)**이다. 반면 준비 단계 자동 수리는 웨이브마다
     * 6회(PREP_TICKS_LATER 90 ÷ STATUS_TICK_INTERVAL 15) × ceil(260×0.04)=11 =
     * **66HP**를 되돌린다. 곧 잘 둔 방어선에서 warrior의 타워 공격은 **수리로 즉시
     * 상쇄되는 크기**다 — 이 종의 위협은 타워가 아니라 방패 3회로 기지까지 걸어가는 쪽에 있다.
     *
     * ⚠ **그래서 수치를 지우지 않았다.** 사전 조사는 `towerAttack` 삭제를 권했지만
     *   사용자 요구가 "남은 애들도 모두 원거리 공격을 하고, 공격할 때는 멈춰서 그
     *   동작을 보여준다"이다. 이격 1칸(사람이 실제로 짓는 자리)에서는 정지 21.9%로
     *   그 연출이 살아 있고, 위 실측이 그것을 잠근다(tests/sim/raiddefense.test.ts).
     *   ⚠ dmg 값 자체는 **검증 불가**다: 0/1/3/6/12 어느 값에서도 전 스테이지 봇 결과가
     *   노이즈 안에서 같고, holdTicks 45/90/180은 결과가 바이트 단위로 동일하다
     *   (정지선 1.7 < 안전 이격 2.0이라 안전 봇 앞에서는 애초에 멈출 일이 없다).
     *   검증되지 않은 값이라는 사실을 여기 적어 둔다 — 흔들 근거도, 지킬 근거도 없다.
     *
     * 개편은 오히려 warrior의 전진을 **덜** 막는다: 옛 규칙에서는 사거리 1.5 안의
     * 타워가 부서질 때까지 무한정 서 있었지만, 이제는 45틱이면 반드시 다시 걷는다.
     * 3 / 1초 = 3dps로 습격대 하단(hexer 1.0 ~ lancer 5.0) 안이다.
     */
    towerAttack: {
      dmg: 3,
      range: 2.2,
      cooldownTicks: 30,
      stopToAttack: true,
      holdTicks: 45,
      ranged: true,
    },
    // 25 → 30: 1단계에서 공짜로 얻은 towerAttack(10dps) 값을 웨이브젠 예산에 반영한다.
    // 방패 3회 + 타워 파괴력을 함께 가진 유닛이 blade(20)보다 싸면 예산 곡선이 밀린다.
    cost: 30,
  },
  shaman: {
    id: 'shaman',
    nameKey: 'enemy.shaman',
    hp: 100,
    speed: 0.9,
    armor: 0,
    flying: false,
    bounty: 24,
    baseDamage: 1,
    radius: 0.34,
    // 주변 힐 — 반경 2, 0.5초마다 hpPerStatusTick × 시전자 hpMul 회복 (자신 제외).
    // hpMul 스케일 덕에 중후반 웨이브에서도 힐러 메커니크가 유효하다 (sim/status.ts).
    healAura: { radius: 2, hpPerStatusTick: 8 },
    cost: 30,
  },
  // --- 부족 습격대 ---------------------------------------------------------
  blade: {
    id: 'blade',
    nameKey: 'enemy.blade',
    // 습격대의 기준점 — 체력 보통, 발 빠름, 제일 먼저 도착해 제일 빨리 던진다.
    hp: 85,
    speed: 1.2,
    armor: 0,
    flying: false,
    bounty: 16,
    baseDamage: 1,
    radius: 0.26,
    /**
     * **투창병** — 짧은 창을 연달아 던진다. 습격대의 기준선이자 최단 사거리(2.4)다.
     * 3 / 0.667초 = 4.5 dps. 간격이 전 종 최소라, 정지 구간에서 holdTicks 75 동안
     * 4발을 넣는다(가장 자주 손이 움직이는 종 = 화면에서 제일 바쁘다).
     * 부수는 속도가 아니라 **붙는 속도**가 정체성이다(speed 1.20, 전 종 최속).
     *
     * ── 10단계: dmg 2 → 3 (정지선 1.7 위에서 다시 잰 값) ─────────────────
     * 이 종이 **거리 축의 판별자**다. 사거리가 2.4로 가장 짧아 이격 1칸과 2칸의
     * 노출 길이 차가 제일 크고(2·√(2.4²−1²)=4.36 대 2·√(2.4²−2²)=2.65, 1.65배),
     * 정지선이 1.7로 내려간 뒤에는 **1칸 타워만** 그 앞에 세운다.
     * 즉 blade의 dmg는 "밀착에 매기는 벌금"의 크기 그 자체다.
     * 실측(스테이지1 · 시드 20 · 덱 spear+catapult+frost · lancer 6 · archer 5 고정,
     * 안전 봇 승수 / 밀착 봇 승수 / 밀착 봇 '웨이브15+ 최소 타워 수' 합):
     *   dmg 2 → 18 / 3 / 160(=상한, 방어선이 안 무너진다)
     *   dmg 3 → 15 / 2 / 153 (최소 2기까지 떨어진다)   ← 채택
     *   dmg 4 →  8 / 1 / 155  (안전 봇까지 무너져 완주 하한 13을 깬다)
     *   dmg 5 →  6 / 1 / 140
     * 3이 "밀착 방어선은 무너지고 이격 방어선은 버틴다"가 처음 성립하는 값이다.
     * dps는 3 / 0.667초 = 4.5로 lancer(5.0) 다음이다 — 연사 담당이라는 자리는 그대로.
     */
    towerAttack: {
      dmg: 3,
      range: 2.4,
      cooldownTicks: 20,
      stopToAttack: true,
      holdTicks: 75,
      ranged: true,
    },
    cost: 20,
  },
  lancer: {
    id: 'lancer',
    nameKey: 'enemy.lancer',
    // 방어형 = 장갑 3. 방패(shieldHits)는 warrior의 정체성이라 겹치지 않게 armor로 준다.
    // 저티어 다단히트 타워(spear 12, poison)가 유독 힘겨워지는 상성이 생긴다.
    hp: 135,
    speed: 0.95,
    armor: 3,
    flying: false,
    bounty: 22,
    baseDamage: 1,
    radius: 0.28,
    /**
     * **큰창잡이** — 무거운 장창을 한 발씩 던진다. 습격대 최장 정지(90)이자 유일한 장갑 종.
     * holdTicks 90은 전 종 최장이라 타워 사거리 안에 가장 오래 서 있는다(규칙 4-a).
     * 장갑 3까지 있어 그 시간을 버틸 수 있는 유일한 종이고, 반대로 다단히트 타워
     * (spear 12, poison)에게는 유독 약하다.
     * blade와 같은 '창'이지만 사거리 0.4가 더 길어 **blade가 못 닿는 자리**에 닿는다.
     *
     * ── 12단계: dmg 6 → **5**. "잴 수 없다"가 더 이상 사실이 아니다 ─────────────
     * 10단계가 이 값을 6으로 올리면서 "스테이지1에 lancer가 없어 직접 잴 수 없다"고
     * 적었고, 그래서 근거가 **서열 추론**(blade dps 4.5보다 높아야 한다)뿐이었다.
     * 12단계는 그 측정 공백을 메웠다 — 스테이지1의 `allowedEnemies`에서 blade 자리에
     * lancer를 끼운 **측정판**(tests/sim/raidmeasure.test.ts)을 만들어 blade와 **같은
     * 잣대**로 잰다. 두 판은 총 HP 곡선도 예산 곡선도 완전히 같다(refHpPerCost가
     * towerAttack 보유 종을 빼고, 두 판의 avgCost가 22.67로 우연히 같다).
     *
     * 실측(측정판 · 시드 20 · 덱 spear+catapult+frost · 별 0.
     * `승 / 여유 / 판당 파괴 / 손실골드`):
     *   기준 blade판 (dmg 3) : 14/20 / **22.0%** / 11.1 / 166,729
     *   lancer dmg 4         : 18/20 / 25.8%     / 10.3 / 157,648
     *   **lancer dmg 5**     : 11/20 / **14.8%** / 11.9 / 166,145   ← 채택
     *   lancer dmg 6         : 10/20 / 11.4%     / 14.8 / 204,313
     * 6은 같은 자리에 세운 blade보다 판을 **10.6%p** 더 조인다 — 습격대 서열의 한 칸
     * 차이가 아니라 다른 등급이다. 5는 그 격차를 7.2%p로 줄이면서 손실골드를
     * blade판과 거의 같은 자리(166,145 대 166,729)로 되돌린다.
     * 4는 여유가 오히려 blade판보다 **높아**(25.8% 대 22.0%) 서열이 뒤집히므로 버렸다.
     *
     * ⚠ **정직하게: 5는 archer와 단발이 같아진다.** 파일 헤더의 네 축 중 '한 방 대
     *   연사'는 이제 lancer 5/36틱 대 archer 5/40틱, 곧 **단발이 아니라 간격**이 가른다
     *   (dps 4.17 대 3.75). "lancer가 dps 최고"는 더 이상 참이 아니다 — blade가 4.5로
     *   위다. lancer를 가르는 것은 dps가 아니라 **장갑 3 · 정지 90틱 · 사거리 2.8**이고,
     *   그 셋은 그대로다.
     */
    towerAttack: {
      dmg: 5,
      range: 2.8,
      cooldownTicks: 36,
      stopToAttack: true,
      holdTicks: 90,
      ranged: true,
    },
    cost: 28,
  },
  archer: {
    id: 'archer',
    nameKey: 'enemy.archer',
    // 유리대포 — 습격대 중 가장 무르다. 스플래시/둔화 한 방에 무리째 정리된다.
    hp: 65,
    speed: 1.05,
    armor: 0,
    flying: false,
    bounty: 21,
    baseDamage: 1,
    radius: 0.24,
    /**
     * **궁수** — 활을 당겨 쏜다. 사거리 3.2로 습격대 상위이고 holdTicks 75.
     * 4 / 1.33초 = 3 dps.
     *
     * 이 종의 값은 여전히 **거리**에 있다. 정지 지점은 전 종 공통 1.7이지만,
     * 사거리 3.2 덕에 1.7까지 걸어 들어오는 동안 1.5타일을 걸으며 쏘고,
     * 무엇보다 **경로에서 3칸 떨어진 타워**에 닿는다 — 전위(blade 2.4 / lancer 2.8)가
     * 못 닿는 자리다. 이격만으로 궁수를 지우려면 4칸까지 물러나야 하고,
     * 그러면 대부분의 타워가 경로를 덮지 못한다(raiddefense.test.ts가 이 대가를 잠근다).
     *
     * ── 아래는 **개편 전 근접 시절의 실측**이다. 이번 개편의 근거이므로 보존한다 ──
     * 근접(칼 1.5 / 창 1.95)은 경로에서 두 칸 떨어뜨리는 것만으로 영구히 무력화된다 —
     * 타워 좌표가 셀 정수라 2.0이 확정 안전선이고, 대부분의 타워는 사거리 2.4~5.5라
     * 거기서도 경로를 덮는다. 3단계 실측에서 **칼잡이 108명의 타워 피해 총합이 0**이었다.
     * 즉 근접만으로는 "부수는 적"이라는 기능이 잘 두는 플레이어에게 존재하지 않는다.
     * 사거리 3.2는 그 안전선(2.0)을 넘으므로, 궁수는 **거리로 풀 수 없는 압박**이고
     * 해답이 배치가 아니라 **대응(먼저 잡기)** 이 된다.
     *
     * 개편 전 값은 dmg 11 / 사거리 3.2 / stopToAttack=false 였고, 아래 스윕으로 잡았다
     * (보상 상한 도입 이후 재측정, 스테이지1 시드 20개, 덱 spear+catapult+frost, 별 0):
     *   dmg  7 → 20/20승 · 기지HP합 197 · 판당 파괴 5.2기 · 손실골드 3,843
     *   dmg  9 → 18/20승 · 180 · 6.9기 · 5,144
     *   dmg 11 → 15/20승 · 171 · 8.8기 · 6,571   ← 당시 채택
     *   dmg 13 → 16/20승 · 133 · 11.9기 · 8,815
     * 이 스윕은 "걸으며 쏘느라 사거리 구간을 지나가는 동안만 쏜다"를 전제로 한 값이라
     * 정지 사격 개편 뒤에는 그대로 쓸 수 없다 — 같은 한 마리가 훨씬 많이 쏘기 때문이다.
     * 그래서 9단계가 4로 내렸고, 그 값을 **잠정치**라고 명시했다.
     *
     * ── 10단계: 4 → 5 (잠정치 해소. 정지선 1.7 위에서 다시 잰 값) ──────────
     * 재측정을 미룬 9단계의 이유("난이도 봉투가 함께 흔들려 원인을 분리할 수 없다")는
     * 정확했다 — 원인은 화력이 아니라 **정지선이 이격 2칸까지 붙잡고 있던 것**이었고,
     * 그걸 먼저 고치고 나서야 이 종의 화력을 단독으로 잴 수 있게 됐다.
     * archer만 움직인 스윕(스테이지1 · **시드 40** · 덱 spear+catapult+frost ·
     * blade 3 고정 · 안전 봇 / 밀착 봇):
     *   dmg  4 → 38/40 · 9/40   dmg  6 → 38/40 · 9/40   dmg  8 → 34/40 · 6/40
     *   dmg 10 → 25/40 · 6/40   dmg 12 → 16/40 · 4/40   dmg 14 →  6/40 · 4/40
     * 곡선이 8까지 거의 평평한 이유는 사거리에 있다: 3.2는 이격 1칸(커버 6.08타일)과
     * 2칸(5.00타일)의 노출 길이 차가 1.22배뿐이라 **거리로 갈리지 않는 화력**이다.
     * 그래서 이 종은 난이도 손잡이로 쓰지 않고, 아래 blade×archer 격자에서
     * "안전 봇이 옛 기준선(16/20)에 가장 가까운" 자리만 고른다 —
     * blade 3 기준 archer 4 → 16/20, **5 → 15/20**, 6 → 13/20(완주 하한과 동률).
     * 5가 옛 기준선에 가장 가까우면서 하한(13)에 여유 2를 남기는 값이다.
     * 5 / 1.33초 = 3.75 dps.
     *
     * ── 2번 복구 라운드: 5 → **6** (공성 화력 재유도) ─────────────────────
     * 봉투 2번(타워 파괴 합계 ≥ 100)이 독립 4블록에서 103/91/79/93으로 셋이 빨갰고,
     * 그 항목 주석이 미리 적어 둔 지침("문턱을 내리지 말고 습격대 화력을 재유도")대로
     * 이쪽을 돌렸다. 유도의 출발점은 **이 봉투에서 공성 손잡이가 둘뿐**이라는 실측이다:
     * 스테이지1 allowedEnemies는 raptor/compy/boar/trike/blade/archer/ptera라
     * **warrior·lancer·hexer는 한 마리도 안 나온다** — 셋의 towerAttack.dmg를 3/3/6/6/2도,
     * 4/3/6/6/2도, 3/3/6/6/3도 돌려 봤는데 파괴 수가 131/111/98/105로 **자릿수까지 동일**했다
     * (무효 손잡이다. 이 봉투를 고치려고 저 셋을 만지지 마라).
     * 남는 것은 blade.dmg 3과 archer.dmg 5뿐이고 정수라 최소 눈금이 +33%/+20%다.
     * blade 쪽은 못 쓴다 — 3→4 한 칸이 파괴를 181/159/139/233으로 날려
     * 봉투 1-a(승수 11~13)와 3번(minT 4~6)을 동시에 무너뜨린다. 그래서 archer 한 칸이
     * 이 봉투에서 **돌릴 수 있는 가장 작은 눈금**이다.
     *
     * 실측(스테이지1 · 덱 spear+catapult+frost · 기준선 봇 · 독립 4블록 × 20시드,
     *  아래는 전부 balance.PLACEMENT_GROWTH 1.11과 **함께** 잰 값이다 — 이 지형은
     *  단조롭지 않아 둘을 따로 재면 안 된다. 그 상수 주석에 짝 스윕 전문이 있다):
     * ```
     *   archer.dmg   [2]파괴 blk1000/2000/5000/9000   [3]minT     [1a]승수
     *   5 (PG 1.13)   103 / 91 / 79 / 93   ✗✗✗       7/6/7/7     15/15/16/16
     *   5 (PG 1.11)   108 / 90 / 97 /118   ✗✗        8/7/7/8     17/17/16/17
     *   6 (PG 1.13)   131 /111 / 98 /105   ✗(blk5000) 7/6/7/7    14/13/14/16
     *   **6 (PG 1.11)  115 /100 /101 /139   ✓ 전부**  8/7/7/6     16/17/16/15
     * ```
     * dps는 6 / 1.33초 = 4.5로 blade와 같아진다 — "궁수가 투창병만큼 아프다"는 말이
     * 이 종의 사거리 3.2(전위가 못 닿는 3칸 자리에 닿는다)와 겹쳐 읽히므로,
     * 이 값을 더 올릴 거면 사거리를 먼저 다시 유도하라.
     * ⚠ 이 한 칸만 단독으로 돌리면(PG 1.13 유지) 봉투 12번(입구 요격)이 깨진다 —
     *   front 55.0% 대 home 62.5%로 격차가 7.5%p뿐이라 문턱 15%p 미달이다(부동소수점이
     *   아니라 진짜 위반). PG 1.11과 짝지으면 60.0 대 77.5로 돌아온다.
     */
    towerAttack: {
      dmg: 6,
      range: 3.2,
      cooldownTicks: 40,
      stopToAttack: true,
      holdTicks: 75,
      ranged: true,
    },
    // 22 → 26: 타워 피해가 +57% 오른 만큼 웨이브젠 예산 단가를 올린다.
    // (안 올리면 습격대 편성이 같은 예산으로 훨씬 강해져 난이도 곡선이 밀린다)
    // ⚠ cost는 난이도 손잡이가 아니다 — 웨이브젠이 총 HP를 정규화하므로 올리면
    //   머릿수가 준 만큼 개체가 튼튼해지고, avgCost를 통해 무관한 웨이브까지 흔든다.
    cost: 26,
  },
  hexer: {
    id: 'hexer',
    nameKey: 'enemy.hexer',
    hp: 85,
    speed: 0.85,
    armor: 0,
    flying: false,
    bounty: 27,
    baseDamage: 1,
    radius: 0.26,
    /**
     * 역할 = **침묵(저주)**. 단순 딜러가 아니라 습격대의 열쇠다.
     *
     * 왜 침묵인가 (다른 후보를 버린 이유):
     *  · '주변 아군 강화'는 shaman(힐 오라)과 같은 서포터 축이라 역할이 겹친다.
     *  · '광역 피해'는 결국 숫자만 큰 딜러다 — 플레이어가 할 일이 안 바뀐다.
     *  · 침묵은 이 게임의 유일한 자원인 **타워의 발사 시간**을 직접 빼앗는다.
     *    저주에 걸린 타워는 살아 있는데도 아무것도 못 하므로, 창잡이가 그 사이 두들긴다 —
     *    "먼저 주술사를 잡을 것인가, 던지는 투창병을 잡을 것인가"라는 표적 선택이 생긴다.
     *    화면에서도 즉시 읽힌다(타워가 조용해지고 보랏빛 룬이 돈다).
     *
     * 수치: 침묵 30틱(1초) / 쿨다운 60틱(2초) = **정확히 절반**.
     * 침묵 중에는 쿨다운도 얼어붙으므로(attack.ts) 잃는 화력이 곧 침묵 시간과 같다 —
     * "주술사 한 명이 붙으면 그 타워는 절반만 일한다"로 딱 떨어지고,
     * 쏘다-멈추다를 반복하므로 무슨 일이 일어나는지가 화면에서 보인다.
     * 중첩되지 않고 max 갱신이라(siege.applySilence) 여럿이 와도 영구 봉쇄는 없다 —
     * 대신 사거리 3.6 안에 계속 머물러야 하므로 결국 처리하면 풀린다.
     * 직접 피해 2/2초 = 1 dps로 습격대 최저 — 부수는 건 어디까지나 창이다.
     *
     * holdTicks 60은 **전 종 최단**이다(warrior 45 제외). 사거리가 제일 길어 가장 먼저
     * 정지 지점에 닿는 종인데 버티는 시간까지 길면 무리 뒤에서 안전하게 침묵만 돌리는
     * 그림이 된다 — 규칙 4-a가 "멈추면 반드시 맞는다"를 보장해도, 짧게 서고 자주
     * 걷게 해야 저주가 **따라다니며 거는 것**이지 눌러앉는 것이 아니게 된다.
     */
    towerAttack: {
      dmg: 2,
      range: 3.6,
      cooldownTicks: 60,
      stopToAttack: true,
      holdTicks: 60,
      ranged: true,
      silenceTicks: 30,
    },
    cost: 34,
  },
  mammoth: {
    id: 'mammoth',
    nameKey: 'enemy.mammoth',
    hp: 900,
    speed: 0.5,
    armor: 6,
    flying: false,
    bounty: 72,
    baseDamage: 3,
    radius: 0.62,
    cost: 90,
  },
  spino: {
    id: 'spino',
    nameKey: 'enemy.spino',
    hp: 2600,
    // 미니보스는 느리고 장갑은 온건하게 — T1~T2 타워로도 집중 사격하면 잡히도록
    speed: 0.5,
    armor: 4,
    flying: false,
    boss: true,
    bounty: 240,
    baseDamage: 5,
    radius: 0.7,
    cost: 300,
  },
  trex: {
    id: 'trex',
    nameKey: 'enemy.trex',
    hp: 6000,
    speed: 0.45,
    armor: 8,
    flying: false,
    boss: true,
    bounty: 480,
    /**
     * 10 → 12 (11단계). **이 게임에서 유일하게 "잘 지어도 통과하는" 몸**의 값이다.
     *
     * 실측이 가리킨 자리다: 최강 정책(tests/sim/botharness.STRONG_BOT)은 스테이지1의
     * 50웨이브 중 **w50의 trex 한 마리에게만** 피해를 입는다. w1~49 누수 피해가 정확히 0이고
     * (판당 spino 0.0 · blade 0.0 · raptor 0.0), 종료 시점 기지 HP가 전 시드에서 25 또는 15로
     * 딱 두 값이었다 — 곧 "trex가 통과했는가"가 **잘 두는 사람에게 남은 유일한 질문**이다.
     * 그 상태에서 여유(잔여HP/최대HP)의 하한은 구조적으로 (25−10)/25 = 60%에 박혀 있었다.
     *
     * 왜 다른 손잡이가 아니라 이것인가 — 같은 −1 여유를 사는 데 드는 기준선(초심자) 비용:
     *   · w50 trex **HP** ↑ (1.38 → 2.4): 최강 −10HP · 기준선 **−0승** ← 먼저 이걸 다 썼다
     *   · trex **baseDamage** ↑ (10 → 12): 최강 −2HP · 기준선 **−0승** (아래 설명)
     *   · 중반 보스 spino 탱크화:          최강 −5HP · 기준선 **−6승**
     *   · w50에 트라이크/멧돼지 추가:       최강 **−0HP** · 기준선 −1~−3승 (전부 요격당한다)
     *   · hpGrowth ↑ (1.020 → 1.026):     최강 −10HP · 기준선 **−7승**
     * 앞의 둘만 기준선에 공짜다. 기전이 같다: **기준선 봇은 이미 이 trex를 못 죽인다**
     * (실측 누수 14/20판 = 살아서 w50에 닿은 판 전부). 이미 내고 있던 값이 조금 오를 뿐
     * 승패가 갈리지 않는다. 반면 최강 봇은 45%만 통과시키던 것이 100%가 된다.
     * 실측(스테이지1 · trex HP 2.4 · 시드 40/20): 여유 60.0% → **52.0%** · 기준선 14/20 그대로.
     *
     * 13 이상으로 가지 않는 이유: 기지 HP가 가장 낮은 스테이지2(20)에서 12는 60%,
     * 13은 65%다. "한 마리에게 판이 끝난다"는 인상은 12에서 이미 충분하다.
     */
    baseDamage: 12,
    radius: 0.8,
    cost: 600,
  },
  golem: {
    id: 'golem',
    nameKey: 'enemy.golem',
    hp: 800,
    speed: 0.55,
    armor: 12,
    flying: false,
    bounty: 88,
    baseDamage: 2,
    radius: 0.5,
    cost: 110,
  },
};

export const ALL_ENEMY_IDS = Object.keys(ENEMY_DEFS) as EnemyId[];
