/**
 * 전역 밸런스 상수 — 데이터/메타/UI가 공유하는 규범 수치.
 *
 * ⚠ 아래 값들은 sim 쪽 하드코딩과 반드시 일치해야 한다 (통합 시 교차 검증):
 *  - SELL_REFUND_RATE       ↔ src/sim/economy.ts SELL_RATE      = 0.6
 *  - SCENERY_CLEAR_*        ↔ src/sim/economy.ts sceneryClearCostFor
 *  - REFRESH_BASE_COST      ↔ src/sim/economy.ts REFRESH_BASE   = 20
 *  - REFRESH_COST_GROWTH    ↔ src/sim/economy.ts REFRESH_GROWTH = 1.6
 *  - HAND_SIZE              ↔ src/sim/economy.ts HAND_SIZE      = 3
 *  - PREP_TICKS_FIRST/LATER ↔ src/sim/battle.ts                 = 150 / 90
 *  - EARLY_CALL_RATE        ↔ src/sim/battle.ts                 = 0.15
 *  - ENDLESS_HP_GROWTH      ↔ src/sim/battle.ts                 = 1.06
 */

import type { EnemyDef, TowerDef, TraitTag, WavePreviewEntry } from './types';

/** 판매 환급률 (invested × 0.6 내림) */
export const SELL_REFUND_RATE = 0.6;
/**
 * **배치 지가 — 새 타워 값은 판이 찰수록 기하급수로 오른다.**
 * 실비용 = round(tiers[0].cost × min(PLACEMENT_GROWTH^n, PLACEMENT_MAX_MUL)), n = 지금 서 있는 타워 수.
 *
 * ══ 왜 선형(옛 PLACEMENT_TAX 0.1)에서 기하로 바꿨는가 — 사용자 진단 ═══════
 * "지금은 타워나 우리 부족을 새로 만드는 것이 업그레이드 하는 것보다 훨씬 싼거 같아."
 * 실측이 정확히 그랬고, 원인은 **두 비용 곡선의 모양이 달랐던 것**이다:
 *     업그레이드  ×2.0 / 티어      (지수 — tests/data가 1.9~2.1로 잠근다)
 *     신축        +10% × 보유수    (선형)
 * 지수 대 선형이라 교차점이 아주 늦게 온다. 손익분기 실측이 **보유 20~23기**인데
 * 스테이지1의 건설 가능 칸은 **84칸**이라, 도달 가능한 전 구간에서 신축이 이겼다.
 * 창움막 5기 보유 시점: 신축 150골드에 +24.0dps(6.3골드/dps) 대 T1→T2 200골드에
 * +14.6dps(13.7골드/dps) = **2.2배**. 맷집은 2.60 대 0.65 HP/골드로 **4배**였다.
 * 즉 고칠 곳은 "업그레이드가 비싸다"가 아니라 **"신축이 충분히 안 비싸진다"**였다.
 *
 * ── 1.13을 고른 계산 ───────────────────────────────────────────────────────
 * 교차점 n* = ln(2 × dps(T1) / Δdps(T1→T2)) / ln(g). 창움막은 2×24 / 14.571 = 3.294이므로
 *     g=1.10 → n* 12.5 · **g=1.13 → n* 9.8** · g=1.15 → n* 8.5
 * 봇 실측 궤적(스테이지1·기본 봇·웨이브별 평균 타워 수)이 w12~19에 정확히 8.0기,
 * w30 11.6기, w46 18.9기(피크)다. 즉 **9~10기가 실제 판단이 일어나는 자리**라
 * 거기에 교차점을 놓으면 "초반엔 넓게, 중반부터 높게"가 처음으로 진짜 선택이 된다.
 * 종별 교차점: 창 9.8 · 투석기 9.8 · 얼음 9.1 · 발리스타 9.1 (전부 한 자리 수 후반).
 *
 * ── 상한(PLACEMENT_MAX_MUL)이 왜 필요한가 ─────────────────────────────────
 * 1.13^n은 n=30에서 38배다(창움막 3,800골드). 그러면 후반에 타워를 **한 기도** 못 늘려
 * 파괴가 곧 사형이 된다 — 봉투 3번('죽음의 나선')이 막으려는 바로 그 상태다.
 * 8배에서 멈추면 창움막 신축 상한이 800골드 = T4 업그레이드 한 번 값이고, 그 지점에서도
 * 골드당 화력은 여전히 업그레이드가 이긴다(신축 0.030 대 T4→T5 0.0436 dps/골드).
 * 8배는 n≈17에서 닿는다 — 봇 피크(18.9기)와 거의 같은 자리다.
 *
 * ══ 2번 복구 라운드: 1.13 → **1.11** (살점 값이 이 곡선 위에서 한 일을 되돌린다) ══
 * 살점 값(bounty를 HP 몫마다 나눠 지급)은 **총액을 한 골드도 안 늘렸는데** 봉투 2번
 * (타워 파괴 합계 ≥ 100)을 독립 4블록에서 123/98/108/127 → **103/91/79/93**으로 깎았다.
 * 인과를 웨이브 밴드별로 갈라 재면 무엇이 먼저 움직였는지가 분명하다
 * (기준선 봇 · 스테이지1 · 독립 4블록 평균 · 웨이브 시작 스냅샷):
 * ```
 *   밴드     판당수입      웨시작 타워수     티어합        파괴수
 *   w1-10    +15.4%       −0.1%            +0.4%         0 → 0
 *   w11-20   + 7.5%       −0.1%            **+19.4%**    0 → 0
 *   w21-30   + 2.3%       +0.9%            + 4.7%        0 → 0
 *   w31-40   + 1.2%       +2.7%            + 2.8%       28 → 31
 *   w41-50   − 2.6%       +1.9%            + 2.9%      428 → 335
 * ```
 * 곧 **돈이 일찍 도착했고, 그 돈이 타워 '수'가 아니라 '티어'로만 갔다**. 이 곡선이
 * 그 이유다 — w11-20에 봇은 이미 8기를 세워 두고 있어(4블록 전부 7.41~7.59기)
 * 신축 한 기가 1.13^8 = 2.66배로 오르는 반면 업그레이드는 값이 그대로다.
 * 남는 돈이 갈 곳이 업그레이드뿐이니 티어가 +19.4% 튀고, TOWER_HP_TIER_GROWTH 1.7이
 * 그 티어 우위를 벽 두께로 굳혀 w41-50의 파괴가 −21.7%가 된다.
 * (채널 분해: 도달 개체 −2.1% · 피격 틱 −4.8% · 타워가 받은 피해 −5.2%인데
 *  **파괴 1기당 필요 피해는 +18.1%** — 곧 '덜 맞은' 것이 아니라 '같은 매를 견딘' 것이고,
 *  내구 채널이 78%를 설명한다)
 *
 * 그래서 고칠 자리는 화력이 아니라 **일찍 온 돈이 흘러갈 수 있는 방향**이다.
 * 1.11이면 8기 시점 배수가 2.66 → 2.30으로 내려가 그 돈이 다시 '수'로도 흐른다.
 * 교차점은 n* = ln(3.294)/ln(1.11) = **11.4** — 봇 실측 궤적의 w20~30 구간(8~11.6기)
 * 안이라 위 '9~10기가 실제 판단이 일어나는 자리'라는 유도가 그대로 산다(1.13은 9.8).
 *
 * 실측 스윕 — archer.towerAttack.dmg 6과 **함께** 돌린 값이다(둘을 따로 재면 안 된다.
 * 이 지형은 단조롭지 않다 — 아래 표의 1.12가 그 증거다). 독립 4블록 × 20시드:
 * ```
 *   PLACEMENT_GROWTH   [2]파괴 blk1000/2000/5000/9000   [3]minT      무수입 공백 최악
 *   1.13 (archer 5)     103 / 91 / 79 / 93   ✗✗✗       7/6/7/7      28.27초
 *   1.13 (archer 6)     131 /111 / 98 /105   ✗(blk5000) 7/6/7/7      28.07초
 *   1.12 (archer 6)     125 / 96 /115 /131   ✗(blk2000) 7/7/7/5     **42.53초 ✗**
 *   **1.11 (archer 6)   115 /100 /101 /139   ✓ 전부**   8/7/7/6      35.40초
 *   1.10 (archer 6)     129 /101 /115 /145   ✓ 전부     8/7/8/8      35.07초 (단 봉투 6번 ✗)
 * ```
 * 1.10을 안 고른 이유는 봉투 **6번(불도저가 지배 전략이 아니다)이 깨지기 때문**이다:
 * 자리가 싸질수록 '소품을 치워 자리를 사는' 쪽도 같이 이득을 보는데, 1.10에서는
 * 일반 68/80·여유 24.15% 대 불도저 69/80·**24.70%**로 승률과 여유가 **동시에** 앞선다
 * (= 그 항목의 '지배 전략' 연언이 참이 된다). 1.11에서는 66 대 67 · 22.95 대 22.95로
 * 여유 다리가 서지 않아 통과한다. 곧 이 상수의 하한을 정하는 것은 봉투 2번이 아니라
 * **6번이고**, 그 방벽은 아래 SCENERY_CLEAR_BASE_COST와의 짝(이 파일 그 주석의
 * "지형 개조는 타워 강화를 포기해야만 가능해 배치 지가 밸런스가 유지된다")이다.
 * 1.10 아래로 더 내리려면 제거비를 **같이** 다시 유도해야 한다.
 *
 * ⚠ 8번(위약 아군 대조)의 여유가 이 값에 민감하다: 위약/진짜 아군 골드비가
 *   1.13 0.7099 · **1.11 0.7047** · 1.10 0.7502 (문턱 0.70). 1.11은 여유 0.47%p뿐이다.
 */
export const PLACEMENT_GROWTH = 1.11;
/** 배치 지가 상한 배수 — 이 위로는 안 오른다 (위 주석 '상한이 왜 필요한가') */
export const PLACEMENT_MAX_MUL = 8;
/**
 * 방해 지형지물(나무·바위) 제거 비용 — n번째 제거(n은 0-base 누적 제거 수)에
 * = min(round(BASE × GROWTH^n), MAX). 환불 없음.
 * (sim/economy.ts sceneryClearCostFor가 임포트해 적용)
 *
 * 튜닝 근거:
 *  · BASE 300 ≈ **T2 업그레이드 한 번**(spear 200 / catapult 240 / lightning 280) 값.
 *    "자리를 하나 사려면 타워 한 기를 한 단계 올릴 돈을 포기한다"가 이 값의 이야기다.
 *
 *    80 → 120 (3단계) → 300 (4단계). 부족 습격대가 들어오면서 **칸의 가치가 올랐다**:
 *    예전에는 소품을 치워 사는 것이 '경로를 더 잘 덮는 자리'뿐이었지만, 이제는
 *    '근접이 닿지 않는 자리'(경로에서 2칸 이상, siege.ts 규칙 1)까지 산다 —
 *    즉 화력뿐 아니라 **타워 수명**을 사는 것이라 같은 값이면 이득이 훨씬 크다.
 *
 *    3단계의 120은 시드 5개로 검증돼 있었고, 넓은 시드에서는 여전히 지배 전략이었다.
 *    재실측(스테이지1, 시드 20개, 덱 spear+catapult+frost, 별 0 — 일반 봇 15/20승·
 *    기지HP합 171·손실골드 131,414 기준):
 *      BASE 120 → 불도저 17/20승·166·제거 20회 (승수 우위 +2 = 지배)
 *      BASE 300 → 불도저 16/20승·164·제거 12회 (승수 +1, 여유는 오히려 감소 = 맞교환)
 *      BASE 470 → 불도저 15/20승·171·제거  0회 (선택지 자체가 죽는다)
 *    300이 "값을 치르면 자리를 살 수 있지만, 사도 결과가 좋아지지는 않는" 지점이다.
 *
 * ── 15단계: **300 → 380. 위의 20시드 판정이 틀렸다.** ────────────────────────
 * 봉투 6·7번의 표본을 20 → 80시드로 넓히자 "사도 결과가 좋아지지 않는다"가 **거짓**으로
 * 드러났다. 20시드에서 +1승은 노이즈로 읽혔지만, 시작점을 옮긴 독립 3벌(각 80시드)에서
 * 부호가 한 번도 뒤집히지 않는다 — 곧 작지만 **실재하는 우위**였다.
 * (승수차 %p / 여유차 %p / 웨평비 / 판 80개의 총 제거 횟수)
 *      BASE 300 → +3.75 · +0.10 · 1.0005 · 63회
 *                 +1.25 ·  0.00 · 1.0023 · 61회
 *                 +7.50 · +0.85 · 1.0028 · 64회   평균 **+4.2%p** = 지배
 *      BASE 380 → +0.00 · −0.45 · 1.0000 · 34회
 *                 +0.00 · −0.10 · 1.0008 · 35회
 *                 +1.25 · +0.05 · 1.0005 · 31회   평균 **+0.4%p** = 대등  ← 채택
 *      BASE 460 → +0.00 ·  0.00 · 1.0000 ·  5회
 *                 +0.00 ·  0.00 · 1.0000 ·  2회
 *                 +0.00 ·  0.00 · 1.0000 ·  2회   제거가 사실상 0 = 선택지 사망
 * 380은 "값을 치르면 자리를 살 수 있고(판당 0.4회 · 160골드), 사도 결과가 대등한"
 * 지점이다. 460은 세 벌 전부 타워 몰빵과 **결과가 완전히 동일**해지는데, 그건 균형이
 * 아니라 상품이 없어진 것이다.
 * ⚠ 이 값은 난이도 손잡이가 아니다 — 기준선 봇은 소품을 한 번도 치우지 않으므로
 *   스테이지 난이도(승수·여유·웨평)는 이 상수에 **한 자리도 반응하지 않는다**.
 *
 * ── 정직성 정리 라운드: **위 유도는 4.5배 낡았고, 이 상품은 지금 죽어 있다** ──────
 * 위 15단계 절의 "380 → 80판에 34/35/31회"를 불도저 봇 80시드 **독립 4블록**
 * (시작점 1000/2000/5000/9000, 공차 37 — 서로소라 완전히 분리된다)으로 다시 쟀다.
 * **잰 트리 = c5ebaad**(그때 `PLACEMENT_GROWTH`는 1.13). 위 GROWTH 절이 적은 대로
 * 이 상수는 배치 지가와 **짝**이므로, 그쪽이 움직였다면 아래를 전부 다시 재라.
 * ```
 *   BASE 380 (지금 값)      blk1000   blk2000   blk5000   blk9000
 *   제거 횟수 / 80판           7         9         6         9     ← 기록은 34/35/31
 *   제거가 일어난 판           7/80      9/80      6/80      9/80
 *   타워 몰빵 승수 / 여유   60·18.50 62·21.45 59·19.75 59·21.10
 *   불도저     승수 / 여유   60·19.05 62·21.45 59·19.75 59·21.10
 *   **판 단위 결과가 같은 판**  78/80     79/80   **80/80**   79/80
 * ```
 * **판당 0.42회가 아니라 0.09회**이고, 위 15단계 절이 460을 두고 "세 벌 전부 타워
 * 몰빵과 결과가 완전히 동일 = 균형이 아니라 상품이 없어진 것"이라 판정한 상태가
 * **380에서 이미 실현돼 있다.** blk5000은 80판의 승패·도달 웨이브·종료 기지HP가
 * 한 자리도 다르지 않은데 그 사이 봇이 소품 6칸을 **2,280골드**에 치웠다 —
 * 골드를 태우고 아무 일도 일으키지 않는다. 승수 동률(네 블록 전부)은 '대등'의
 * 증거가 아니라 **같은 판을 밟는다**는 증거다.
 * (언제 밀렸는지는 불명이다. 이 상수는 그때 이후 한 번도 안 바뀌었으므로 움직인 것은
 *  주변이다 — 배치 지가·살점 값·마을 비용 표·상성 개편이 전부 그 뒤에 들어왔다)
 *
 * ── 값을 안 내렸다. 프론티어를 재 두고 **별도 과제로 넘긴다** ────────────────
 * 같은 하네스·같은 4블록으로 값만 갈아 끼운 스윕(지형 팔만. 기준선은 위 ⚠대로 불변):
 * ```
 *   BASE   제거 횟수/80판(4블록)    지배 연언(승수>·여유> 동시)   승률 상한 +5%p
 *   160    145 / 147 / 151 / 156    **true 4/4**                  **false 4/4**
 *   240     92 /  82 /  83 /  87    **true 4/4**                  **false 3/4**
 *   300     46 /  34 /  40 /  42    **true 2/4**(blk2000·5000)     true 4/4
 *   340     24 /  23 /  19 /  26      false 4/4                    true 4/4
 *   360     18 /  16 /  13 /  15      false 4/4                    true 4/4
 *   380      7 /   9 /   6 /   9      false 4/4                    true 4/4   ← 지금 값
 * ```
 * 곧 "살아 있으면서 지배가 아닌" 창은 **300과 380 사이**이고, 옛 유도가 겨냥한
 * 판당 0.4회에 가장 가까운 것은 **340**(판당 0.24~0.33회)이다.
 * ⚠ 그런데 340은 **봉투 6번의 여유 다리를 두 블록에서 켠다**(blk5000 20.20 > 19.75 ·
 *   blk9000 21.65 > 21.10, 둘 다 승수 동률 59/59). 지금(380)은 그 다리가 커밋 블록
 *   하나뿐이다. 곧 상품을 되살리면 **여유 0짜리 연언이 1/4에서 2/4로 늘어난다** —
 *   값을 내리기 전에 그 연언(`>` 두 개, 노이즈 폭 없음)을 먼저 다시 유도해야 한다.
 * **그래서 이 라운드는 값을 한 자리도 안 건드렸다.** 죽은 상품을 되살리는 것은
 * 기록 정리가 아니라 밸런스 결정이고, 그 결정은 봉투 6·7번의 연언 재유도와 **같이**
 * 해야 한다. 다음 사람이 할 일은 순서대로 셋이다:
 *   ① 6·7번의 '지배 전략' 연언을 측정 분해능 폭이 있는 형태로 다시 유도한다.
 *   ② 그 위에서 340(또는 그 근처)으로 내리고 위 4블록을 전부 다시 잰다.
 *   ③ 봉투 7번의 지형 갈래 웨평비 하한(0.93)도 같이 확인한다(지금 1.0000~1.0003).
 *
 *  · GROWTH 1.6 = 새로고침 곡선과 같은 기울기. 개별가 380/608/973/1557/2490/3984…,
 *    누적 380/988/1961/3518/6008/9992…로 **2회째 누적이 스테이지1 웨이브 1~20 총 보상
 *    (1,860골드)을 이미 넘는다**. 스테이지1 소품은 40칸이라 '맵을 민다'는 선택지가 없다.
 *    → 지형 개조는 타워 강화를 포기해야만 가능해 배치 지가(PLACEMENT_GROWTH) 밸런스가 유지된다.
 *      실측: 불도저 봇(제거 우선)이 스테이지1에서 일반 봇보다 우세하지 않고
 *      스테이지6은 여전히 클리어 불가 (tests/sim/autoplay.test.ts).
 *  · MAX 4000 = 무한 모드 후반 골드 인플레에서도 지수가 오버플로하지 않게 하는 상한.
 *    (T5 업그레이드 1600~2400보다 비싸 후반에도 '도배'가 이득이 되지 않는다)
 */
export const SCENERY_CLEAR_BASE_COST = 380;
export const SCENERY_CLEAR_GROWTH = 1.6;
export const SCENERY_CLEAR_MAX_COST = 4000;

/** 핸드 새로고침: 웨이브당 1회 무료, 이후 20 × 1.6^n 반올림 */
export const REFRESH_BASE_COST = 20;
export const REFRESH_COST_GROWTH = 1.6;
/** 핸드 크기 (카드 3장 유지) */
export const HAND_SIZE = 3;
/** 준비 단계 카운트다운 (웨이브1 전 / 이후) */
export const PREP_TICKS_FIRST = 150;
export const PREP_TICKS_LATER = 90;
/** 조기 웨이브 호출 보너스: 남은 prep 틱 × 0.15 골드 (내림) */
export const EARLY_CALL_RATE = 0.15;
/** 무한 모드: waveCount 초과 웨이브당 추가 HP 성장 */
export const ENDLESS_HP_GROWTH = 1.06;

// ---------------------------------------------------------------------------
// 타워 구조물 체력 — 적 부족(사람)의 공격 대상 (sim/siege.ts가 소비)
//
// 수치 체계 (toughness 1.0 기준):
//   T1 260 / T2 390 / T3 585 / T4 878 / T5 1316
//   = TOWER_HP_BASE × TOWER_HP_TIER_GROWTH^tier, 별 1개당 +6%.
//
// 튜닝 근거:
//  · 260 = 근접 부족원(dmg 10 / 1초)이 혼자 26초를 두들겨야 T1 하나를 부수는 값.
//    한 명은 위협이 아니고 **무리**여야 위협이 된다는 게 이 메커니즘의 핵심이다
//    (6명이 붙으면 4.3초 — 플레이어가 반응할 시간은 있지만 방치하면 잃는다).
//  · 성장률 **1.5 → 1.7 (9단계)**. 여전히 티어 cost 성장(약 2.0)보다 낮아 "비싼 타워일수록
//    HP 효율은 나쁘다"는 성질과 tests/data의 상한(cost 성장 하한 1.9 미만)은 그대로다.
//    올린 이유: 사용자 진단("새로 짓는 게 업그레이드보다 훨씬 싸다")의 **맷집 쪽 절반**이다.
//    1.5일 때 신축은 2.60 HP/골드인데 T1→T2는 0.65로 **4배** 차이였다 — 화력(2.2배)보다
//    더 벌어져 있었고, 습격대가 타워를 부수는 이 게임에서 그건 업그레이드를 두 번 벌하는 셈이다.
//    1.7이면 T5가 1,316 → 2,171로 올라 "높은 타워는 실제로 안 부서진다"가 성립한다.
//  · 별(메타 진행) 보너스는 dmg/rate(8~10%)보다 얕은 6%다. 별은 화력을 사는 것이지
//    맷집을 사는 것이 아니며, 별 5개(+30%)로도 무리 앞에서는 결국 부서진다.
// ---------------------------------------------------------------------------
/** T1 기준 최대 HP (toughness 1.0) */
export const TOWER_HP_BASE = 260;
/** 티어당 최대 HP 배율 */
export const TOWER_HP_TIER_GROWTH = 1.7;
/** 별 1개당 최대 HP 증가율 */
export const TOWER_HP_PER_STAR = 0.06;
/**
 * 준비 단계(prep) 자동 수리 — STATUS_TICK_INTERVAL(0.5초)마다 maxHp의 이 비율만큼 회복.
 * 4% × 초당 2회 = 초당 8%. 웨이브 사이 prep 90틱(3초)에서 24%가 돌아온다.
 * prep에는 살아 있는 적이 0마리인 것이 시뮬레이션상 보장되므로(웨이브 완료 조건)
 * "수리 중 두들겨 맞는" 애매한 상태가 생기지 않는다.
 * 이 회복이 없으면 타워는 웨이브를 거듭할수록 무조건 죽는 소모품이 되어
 * 업그레이드 투자가 무의미해진다 — 수리는 손실을 되돌리는 게 아니라 늦추는 장치다.
 */
export const TOWER_REPAIR_PER_STATUS_TICK = 0.04;

// ---------------------------------------------------------------------------
// 정지 사격 (습격대가 멈춰 서서 타워를 쏜다) — src/sim/siege.ts 규칙 4가 소비
//
// 규칙 전문과 스톨 방지 논증은 siege.ts 헤더에 있다. 여기 있는 건 **판의 기하가
// 정하는 공통 수치** 셋뿐이다 — 종별 취향(holdTicks)은 TowerAttackSpec에 있다.
// ---------------------------------------------------------------------------
/**
 * 정지 판정 거리 (타일) — 습격대는 목표 타워까지의 거리가 이 값 이하로 들어오면
 * 멈춰 선다. **종별 취향이 아니라 판의 기하가 정하는 공통값**이다.
 *
 * ── 10단계(밸런스): 2.1 → 1.7 ─────────────────────────────────────────────
 * 이 값이 정하는 것은 "얼마나 자주 멈추나"가 아니라 **어느 칸에 지은 타워가
 * 붙잡히나**다. 타워 좌표가 셀 정수라 경로 이격은 사실상 1칸·2칸 두 자리뿐이고
 * (스테이지1 배치 가능 84칸의 거리 분포: 1.0×49 · 1.5×3 · 2.0×21 · 2.3×4 · 3.0×7),
 * 이 값이 그 둘 사이 어디에 놓이느냐가 siege.ts 규칙 1의 약속
 * — "경로에서 얼마나 떨어뜨렸는가가 곧 위험도" — 을 지키느냐 마느냐를 가른다.
 *
 * **2.1은 그 약속을 깼다.** 두 칸(2.0)도 정지 구간 안이라 1칸과 2칸이 똑같이
 * 붙잡혔고, 전원 원거리 개편으로 사거리(2.2~3.6)까지 두 자리를 모두 덮으므로
 * **거리가 아무것도 사지 않는 상태**가 됐다. 남는 차이는 커버 길이뿐인데 그건
 * 가까울수록 크다 → 밀착이 오히려 유리해진다. 실측(스테이지1 · 시드 20 ·
 * 덱 spear+catapult+frost · 안전 봇 = 이격 2 / 밀착 봇 = 이격 1,
 * 정지 사격 비율 = 정지 중 발사 / 전체 발사):
 *    2.1 → 안전 43.6% · 밀착 43.1%   (둘 다 붙잡힌다 = 거리 축이 죽었다)
 *    1.9 → 안전  1.2% · 밀착 42.8%
 *    1.7 → 안전  1.2% · 밀착 42.4%   (2칸은 못 붙잡고 1칸만 붙잡는다)
 *  같은 A/B의 판 결과: 2.1에서 안전 봇 18승 · 파괴 211기 · 손실골드 157,813,
 *  1.7에서 18승 · 116기 · 86,782 (밀착 봇은 둘 다 3승).
 *  곧 이 값을 내린 것은 **밀착을 봐준 게 아니라 이격을 되살린 것**이고,
 *  그 되살린 축 위에서 습격대 화력을 다시 잡았다(enemies.ts blade·archer 주석).
 *
 * 왜 1.7인가 (1.0 링과 2.0 링 사이에서 양쪽 모두와 멀다):
 *  · 2.0 링과 0.3 여유 → 두 칸 이상 떨어뜨린 타워 앞에는 **어떤 종도** 못 선다.
 *  · 1.0 링 주위로 폭 2·√(1.7²−1.0²) = **2.75타일**의 정지 구간이 열려 어떤 speed로도
 *    반드시 그 안에 들어간다 (2.1이 두 칸에 열어 주던 1.28타일보다 넓다).
 *  · 코너에서 생기는 1.5 링도 폭 1.60타일로 여전히 붙잡힌다 — "모서리에 붙이면
 *    공짜"라는 구멍이 생기지 않는다.
 *  값이 종별 사거리(최단 warrior 2.2)보다 짧으므로 stopDistFor의 min에서 언제나
 *  이 값이 이긴다 → "정지 거리는 전 종 공통"이라는 성질도 그대로다.
 *
 * ── 옛 주석 (2.1의 유도 — 지우지 않는다. 위 '0.3 여유'의 근거가 이것이다) ──
 *  "정확히 2.0으로 잡으면 경로에서 두 칸 떨어진 타워와의 거리가 **정확히 2.0** —
 *   즉 정지 판정이 부등호의 칼날 위에 선다. 적은 매 틱 speed×TICK_DT 만큼 이산적으로
 *   뛰므로, 그 한 점을 밟는지는 종별 speed와 스폰 위상에 달린 **우연**이 된다.
 *   실측(직선 경로 · 이격 2에 타워 4기(간격 4) · 각 종 8마리):
 *     2.0 → blade 33% · lancer **0%** · archer **0%** · hexer **0%** · warrior 50% (합 21%)
 *     2.1 → blade 43% · lancer 33% · archer 35% · hexer 27% · warrior 25% (합 34%)
 *   2.0에서는 다섯 중 셋이 **한 발도** 정지 사격을 못 했다. (…) 데이터로 표현되지
 *   않는 규칙은 규칙이 아니다."
 *  → 그 요구는 지금도 유효하고, 1.7은 그것을 **1칸 링에 대해** 만족시킨다.
 *    tests/sim/siege.test.ts '5종 전부 정지 사격을 한다'가 이격 1에서 이 값을 잠근다.
 */
export const SIEGE_ENGAGE_RANGE = 1.7;
/**
 * 정지 뒤 **의무 전진 틱** — 한 번 멈춘 습격대는 정지가 끝난 사유와 무관하게
 * 이만큼 실제로 전진하기 전에는 다시 멈추지 못한다 (siege.ts 규칙 4-b).
 *
 * 이 상수 하나가 스톨(전선 영구 정체)을 **구조적으로** 불가능하게 만든다.
 * 최장 정지(holdTicks 90)와 짝지어도 전진 이동 틱 비율이
 * W/(H+W) = 120/210 ≈ 57% 아래로 내려갈 수 없다 — 예외가 0개이기 때문이다.
 * (봉쇄·스턴으로 못 걷는 틱에는 이 잔여가 줄지 않으므로 비율은 더 보수적으로만 간다)
 */
export const SIEGE_ADVANCE_TICKS = 120;
/**
 * 타워를 향해 무기를 놓는 동작의 **최대** 길이 (틱). 실제 길이는
 * min(RAID_ATTACK_ANIM_TICKS, cooldownTicks)라 연사가 빠른 종도 동작이 겹치지 않는다.
 * 시뮬레이션 판정에는 전혀 쓰이지 않고 EnemyState.attackAnimLeft/attackAnimTicks와
 * raidAttack 이벤트로만 나간다 — 렌더가 진행도 = 1 − left/ticks 로 읽는다.
 */
export const RAID_ATTACK_ANIM_TICKS = 12;

/**
 * **문간 한 입의 주기** (틱). 보스가 마을 문 앞에 서서 무는 간격 — 30틱 = 1초.
 *
 * 왜 1초인가: 이 값이 정하는 것은 DPS가 아니라 **플레이어가 반응할 수 있는 단위**다.
 * 문간 공성의 계약(src/sim/gate.ts 규칙 3)이 "첫 1초에 죽이거나 봉쇄하면 피해 0"인데,
 * 주기가 이보다 짧으면 그 약속이 화면에서 지켜지지 않고(반응 전에 이미 두 입) 길면
 * 마을 HP 게이지가 몇 십 초씩 멈춰 있어 "지금 물리고 있다"가 안 읽힌다.
 * 정확히 1초라 HUD가 초 단위 카운트다운을 그대로 그릴 수 있다(2단계 (b)).
 */
export const GATE_BITE_TICKS = 30;
/**
 * **문간 한 입의 크기** = `ceil(baseDamage / GATE_BITE_DIVISOR)`.
 *
 * 곧 이 값은 "보스를 문간에 몇 초 방치하면 종전의 누수 한 번과 같은 값을 치르는가"다.
 * 4면 4초, 8이면 8초. 적 데이터(baseDamage)를 **한 값도 안 바꾸고** 문간의 세기만
 * 여기서 정한다 — 종의 서열(trex 12 > spino 5 > compy 3…)이 그대로 문간에 상속된다.
 *
 * ⚠ **왜 나눗셈이고 왜 올림인가.** 곱셈 상수(초당 N)로 두면 종 서열이 지워져 문간에서
 * compy와 trex가 같은 무게가 된다. 올림은 하한 1을 공짜로 준다 — 곧 어떤 divisor에서도
 * "한 입은 반드시 1 이상"이라 gate.ts 의 교착 불가능성 증명(마을 HP 카운트다운)이
 * 이 상수에 의존하지 않는다.
 *
 * 값 4 = trex(12) 3/초 → 스테이지1 마을 25가 **8.3초** · spino(5) 2/초 → 12.5초.
 * 실측 스윕은 src/sim/gate.ts 헤더의 표를 보라(4/6/8 3점).
 */
export const GATE_BITE_DIVISOR = 4;

/** 티어/별/내구도 배율 → 타워 최대 HP (정수). sim과 UI가 같은 함수를 쓴다. */
export function towerMaxHpFor(tier: number, stars = 0, toughness = 1): number {
  const t = Math.max(0, Math.floor(tier));
  return Math.max(
    1,
    Math.round(TOWER_HP_BASE * TOWER_HP_TIER_GROWTH ** t * (1 + stars * TOWER_HP_PER_STAR) * toughness),
  );
}

// ---------------------------------------------------------------------------
// 홈타운(기지) — 싸우고 성장하는 최후 방어선 (sim/hometown.ts가 소비)
//
// 레벨 테이블 자체와 수치 근거는 src/data/hometown.ts에 있다. 여기 있는 건
// **최대 HP를 정수로 확정하는 한 줄**뿐이다 — sim과 UI가 같은 함수를 써야
// 패널에 뜬 숫자와 실제 체력바가 어긋나지 않는다 (towerMaxHpFor와 같은 이유).
// ---------------------------------------------------------------------------
/**
 * 홈타운 최대 HP = round(스테이지 baseHp × 레벨 hpMul), 최소 1.
 * 정수로 확정하는 이유는 타워 수리와 같다 — hp가 부동소수가 되면 hash()가
 * 마지막 자리에서 흔들려 결정론 검증이 무의미해진다.
 */
export function baseMaxHpFor(stageBaseHp: number, hpMul: number): number {
  return Math.max(1, Math.round(stageBaseHp * hpMul));
}

// ---------------------------------------------------------------------------
// 아군 부족 유닛 — 마을에서 골드로 출동시키는 소모품 전력 (sim/allies.ts가 소비)
//
// 설계 근거 전문은 src/sim/allies.ts 헤더에 있다. 여기 있는 건 **경제 손잡이**뿐이다.
// ---------------------------------------------------------------------------
/**
 * 동시 출동 상한. 이 수에 도달하면 trainAlly가 거부된다(골드가 남아도).
 *
 * 왜 상한이 필요한가:
 *  · 렌더 — 아군은 자기 InstancedMesh 하나(정원 CAPACITY 100)에 모인다. 6명은
 *    그 정원에도, 삼각형 예산(150,000)에도 여유가 큰 크기다 — 실측 최악 프레임에서
 *    아군 6명의 몫은 약 7,400 삼각형·드로우콜 +1이다. 무제한이면 정원이 곧 밸런스가 된다.
 *  · 밸런스 — 상한이 없으면 후반 골드 인플레가 그대로 무한 방벽이 된다. 6명은
 *    "한 길목을 잠깐 막는다"는 크기이지 "전선을 만든다"는 크기가 아니다.
 */
export const ALLY_MAX_ACTIVE = 6;
/**
 * 출동 비용 성장 — 실비용 = round(def.cost × ALLY_COST_GROWTH^(현재 나가 있는 인원)).
 * 배치 지가도 9단계부터 기하급수라 이제 둘이 같은 모양이다. 아군 쪽이 되돌아오는 이유: 죽으면
 * 인원이 줄어 비용이 **되돌아온다**. 되돌아오는 세금이 선형이면 6명 유지가 너무 싸다.
 *
 * 1.35 → 1.20 (4단계). 근거는 **가동률**이다: 실전에서 아군이 적을 실제로 붙잡고 있는
 * 시간은 생존 시간의 **1~7%**뿐이다(botharness의 allyBlockTicks/allyTicks — 5단계에서
 * 정정된 값이다. 4단계 문서의 "4~13%"는 지표가 **봉쇄당한 적의 마릿수**를 세고 있어
 * 1.5~2배 부풀려진 수치였다. 정정 실측: 스테이지1 이긴 판 1.1% · 진 판 3.5% ·
 * 무한 2.7% · 유닛 몰빵 7.1%). 잘 막고 있는 판일수록 마을 앞까지 오는 적이 없어
 * 아군은 서 있다가 수명이 다한다 — 이건 아군이 약해서가 아니라 **보험이라서** 그렇다.
 * 지수 1.35는 그 위에 "여섯을 한꺼번에 내보내는 순간"까지 벌금을 물려, 정작 위급할 때
 * 정원을 채우는 비용이 794골드(T2 업그레이드 두어 번)가 됐다. 1.20이면 같은 여섯이
 * 398골드다. 상한(ALLY_MAX_ACTIVE 6)이 이미 화력 천장을 잡고 있으므로 지수가
 * 이중으로 같은 일을 할 이유가 없다.
 *
 * ── 1.20 → **1.05** (9단계). 영구화가 이 지수의 뜻을 바꿔 놓았다 ────────────
 * 위 문단이 지수를 정당화한 근거는 **"죽으면 인원이 줄어 비용이 되돌아온다"**였다.
 * 그런데 9단계에서 아군이 수명 20초짜리 소모품에서 **영구 유닛**이 되면서, 이 지수는
 * 되돌아오는 세금이 아니라 **정원을 채우는 일회성 관문**이 됐다. 몽둥이꾼(9단계 가격 90)
 * 기준 실비용은 1.20에서 90/108/130/156/187/224 = **한 세트 895골드**로,
 * 스테이지1 시작 골드의 3배이자 T4 업그레이드 한 번 값이다. 곧 마을이 정원을 6까지
 * 팔아도 **그 정원을 채울 수가 없었다.**
 *
 * 그 결과가 난이도 봉투 **11번(정원 곡선)**의 적색이었다. 그 항목은 마을 성능을 양 팔 다
 * Lv1에 못 박고 **정원만** 2와 6으로 격리해 "마을이 판 머릿수가 실제로 배달되는가"를
 * 재는데, 정원 6 팔이 정원 2 팔보다 겨우 1.19배만 붙잡았다(문턱 1.25).
 * 기전은 **포화**다 — 정원 2→6에서 서 있는 아군 인원틱은 2.4배가 되는데 붙잡는 양은
 * 1.05배뿐이고, 한계 4명이 추가로 붙잡은 양이 51,668 → 20,520(−60%)으로 무너진 반면
 * 처음 두 명의 몫(104,859 → 108,031)은 그대로다. 곧 병목은 아군의 힘이 아니라
 * **한계 인원을 아예 못 뽑는 것**이었고, 그걸 정하는 손잡이가 여기다.
 *
 * 1.05면 같은 여섯이 90/95/99/104/109/115 = **612골드**다(−32%). 상한
 * (ALLY_MAX_ACTIVE 6)이 이미 화력 천장을 잡고 있으므로 지수가 이중으로 같은 일을 할
 * 이유가 없다는 위 문단의 논리를 한 번 더 적용한 것이고, 지수 > 1은 유지되므로
 * "여섯을 한꺼번에 내보내면 더 비싸다"는 성질 자체는 남는다.
 *
 * ── 실측 (스테이지1 · 봉투 11번(b)와 같은 식 · 40시드 블록 4벌 = 160시드) ──
 *              생산비(정원6/정원2)   봉쇄비          블록별 봉쇄비
 *   1.20        1.180               **1.190** ✗    1.045 / 1.346 / 1.249 / 1.149
 *   1.05        1.294               **1.285** ✓    1.159 / 1.359 / 1.365 / 1.302
 * 문턱은 생산 1.10 · 봉쇄 1.25다. 블록 간 분산도 같이 줄었다(폭 0.301 → 0.206).
 * ⚠ 이 값을 되돌리면 봉투 11번이 **표본을 아무리 늘려도** 다시 빨개진다 — 같은 항목을
 *   타워 사거리로 고치려 한 시도는 전부 실패했고(사거리는 이 비에 **단조가 아니다**:
 *   폭 100% → 1.190 · 70% → 1.149 · 50% → 1.393), 게다가 타워를 약하게 만들어
 *   봉투 3·6·12번을 연쇄로 깨뜨렸다. 고칠 곳은 타워가 아니라 **한계 아군의 값**이었다.
 */
export const ALLY_COST_GROWTH = 1.05;
/*
 * ── 출격 한계선 3형제는 삭제됐다 (9단계) ──────────────────────────────────
 * ALLY_SORTIE_RANGE(6.0) · ALLY_SORTIE_PATH_LIMIT(0.5) · ALLY_HOLD_SPACING(0.5).
 * 사용자가 "반경 제한 없이 맵 어디든 찍을 수 있게"로 재정의해 한계선 자체가 없어졌고,
 * 대기 줄도 없어졌다(아군이 줄을 서지 않고 찍은 자리로 흩어진다).
 * 그것들이 하던 억제는 두 곳으로 갈라져 옮겨갔다 — 자세한 것은 src/sim/allies.ts 헤더:
 *   · **죽는다** (수명 대신 HP가 유일한 목숨. 앞에 세울수록 빨리 죽는다)
 *   · **정원을 마을이 정한다** (BaseLevelDef.allyCap — 예전 sortie가 있던 그 칸)
 */

/**
 * 집결 지점이 홈타운에서 얼마나 앞으로 나오는가 (타일).
 *
 * "홈 타운 바로 앞"(사용자 지시 ②)의 수치화다. 1.4는 기지 셀 자신(반경 0.5쯤을 차지한다)과
 * 겹치지 않으면서 한 칸을 넘지 않는 크기 — 즉 화면에서 **마을 문 앞**으로 읽힌다.
 * 더 크면 "마을 앞"이 아니라 "길에 나가 있는" 그림이 되고, 더 작으면 지붕에 겹친다.
 */
export const ALLY_MUSTER_FORWARD = 1.4;
/** 집결 대열의 가로 칸 수 — 3열이면 정원 6명이 3×2로 선다 */
export const ALLY_MUSTER_COLS = 3;
/**
 * 집결 대열의 칸 간격 (타일). 유닛 충돌이 없어서 안 벌리면 전원이 한 점에 포개진다
 * (8단계 실측: 6명이 (4.86, 11) 한 점에 겹쳐 한 명처럼 보였다).
 * 0.6이면 3×2 대열이 1.2 × 0.6타일을 차지해 기지 셀 한 칸 폭 안에 들어온다.
 */
export const ALLY_MUSTER_SPACING = 0.6;
/**
 * 적의 난투(반격) 피해 유도 계수 — EnemyDef.brawl이 없을 때 cost에서 유도한다.
 * cost는 이미 이 게임의 전투력 지표(웨이브젠 예산 단가)라 종별 서열이 그대로 옮겨 온다:
 * compy(5)→2 · raptor(10)→6 · blade(20)→11 · trike(45)→25 · trex는 그 이상.
 * 0.55는 "몽둥이꾼이 랩터 **한 마리**에게는 수명(20초)을 다 채우고, **세 마리**에게
 * 붙잡히면 수명 안에 쓰러진다"를 만드는 값이다 — 곧 이 계수가 정하는 것은 절대 시간이
 * 아니라 **정원 봉쇄(ALLY_BLOCK_CAPACITY 3)와의 관계**다. 실제 값: 난투는 30틱마다
 * 도므로 랩터 한 마리 = 6/초이고, 몽둥이꾼 hp 240은 1마리 앞 40초(수명의 2배) ·
 * 3마리 앞 13.3초(수명의 67%)다. (3단계의 hp 140에서는 23.3초 · 7.8초였다 —
 * 한 마리 쪽은 두 값 모두 수명을 넘으므로 이 계수가 사려던 성질은 그대로다)
 */
export const BRAWL_DMG_PER_COST = 0.55;
/** 난투 타격 간격 (틱). 타워 공격 쿨다운과 **따로** 돈다 */
export const BRAWL_COOLDOWN_TICKS = 30;
/**
 * 근접 아군 한 명이 동시에 붙잡아 둘 수 있는 적의 수 (sim/allies.ts 규칙 5-b).
 * 붙잡은 적 하나하나가 전부 그 아군을 난투로 때리므로, 이 값은 곧
 * **"얼마나 넓게 막는가"와 "얼마나 빨리 죽는가"를 동시에 정하는 하나의 손잡이**다.
 *
 * ── 왜 1이면 안 되는가 (4단계 실측) ────────────────────────────────────────
 * 1마리 고정에서는 아군이 **어떤 수치를 줘도 무의미했다**. 무한 모드(스테이지1, 시드 12)
 * 도달 웨이브로 잰 A/B에서 dmg×2 · hp×2 · 사거리×1.6 · 비용×0.4 · 수명×3 ·
 * 상한 6→16 · 출격 한계선 6→16을 전부 따로 돌렸는데 결과가 50.8~55.4 사이에 뭉쳐
 * 타워만 쓰는 기준선(55.8)을 **한 번도 넘지 못했다**. 원인은 화력이 아니라 가동률이었다:
 * 실전에서 아군이 적을 붙잡고 있는 시간은 전체 생존 시간의 **1~7%**뿐이다
 * (통제된 직선 실험장에서는 41~63%). 잘 막은 판일수록 마을 앞까지 오는 적이 없어
 * 아군은 그냥 서 있다가 수명이 다한다. 즉 1:1 봉쇄는 "여섯 명이 여섯 마리를 세운다"인데,
 * 웨이브가 60마리라 애초에 살 수 있는 시간의 총량이 너무 작았다.
 *
 * ── 왜 무제한이면 안 되는가 ────────────────────────────────────────────────
 * 사거리 안 전부를 막게 하면 몽둥이꾼 **한 명이 웨이브 전체를 영구히 세운다**
 * (실측: 동시 봉쇄 21마리, 모든 구성에서 누수 0, 웨이브가 2,400틱 안에 끝나지 않음).
 * 55골드짜리가 게임을 멈추는 버튼이 된다.
 *
 * ── 3인 근거 ───────────────────────────────────────────────────────────────
 * 반격이 인원수만큼 겹치므로 3은 "붙잡은 만큼 맞는다"가 실제로 아프기 시작하는 값이다:
 * 몽둥이꾼(hp 240)이 랩터 3마리(각 6피해/초)에게 18dps를 받아 13.3초 만에 죽고
 * (수명의 67% — 3단계의 hp 140에서는 7.8초 = 39%였고, **그 격차를 정원 3에 맞춰
 * 다시 잡은 것이 단계 7의 hp 140 → 240이다**),
 * 파수꾼(hp 560 · armor 3)은 같은 상황에서 9dps라 수명(20초)을 채운다.
 * 즉 이 값 하나가 **"몽둥이꾼은 잠깐 넓게, 파수꾼은 오래 좁게"**라는 두 종의 차이를
 * 비로소 성립시킨다 — 그전까지 파수꾼은 그냥 화력이 낮은 몽둥이꾼이었다.
 */
export const ALLY_BLOCK_CAPACITY = 3;
/**
 * 규칙 5-c) **스치는 타격**의 팔 길이 (타일) — 봉쇄가 없어도 이 거리 안의 아군은 맞는다.
 *
 * 9단계 신설. 영구화 전에는 원거리 아군(무릿매)을 때릴 수 있는 적이 하나도 없었고,
 * 수명 20초가 그 사실을 가리고 있었다 — 영구가 되는 순간 **불멸 유닛**이 된다.
 * 근거 전문은 src/sim/allies.ts brushTarget 주석.
 *
 * 1.1을 고른 이유: 근접 아군의 봉쇄 사거리(몽둥이꾼 1.0 · 파수꾼 1.15)와 같은 자리다.
 * 더 짧으면 무릿매가 근접 아군 뒤 반 칸에 숨어 여전히 불멸이고, 더 길면 길에서
 * 비켜 세운 유닛까지 맞아 "위치가 판단"이라는 이 규칙의 목적 자체가 사라진다.
 */
export const BRAWL_BRUSH_RANGE = 1.1;
/*
 * ── ALLY_RETIRE_REFUND(0.5) 는 삭제됐다 (9단계) ───────────────────────────
 * 수명이 없어져 "살아 돌아오는" 사건 자체가 사라졌다 — 부족원이 리스트에서 빠지는 길은
 * 이제 맞아 죽는 것 하나뿐이고, 쓰러진 아군은 8단계에도 한 푼도 돌려주지 않았다.
 * 그래서 이 상수는 값을 바꾼 게 아니라 **적용될 자리가 없어졌다**.
 *
 * 이 상수가 사던 것("혹시 몰라 미리 내보낸 것을 벌하지 않는다")은 지금 공짜로 얻는다:
 * 미리 뽑아 둔 부족원은 20초 뒤에 사라지지 않고 **계속 서 있다**. 미리 내보내는 것이
 * 손해였던 이유 자체가 없어진 것이다.
 */



/** 지금 한 명 더 내보내는 실비용 (aliveCount = 현재 나가 있는 인원) */
export function allyCostFor(baseCost: number, aliveCount: number): number {
  return Math.round(baseCost * ALLY_COST_GROWTH ** Math.max(0, aliveCount));
}

/** EnemyDef.brawl 생략 시의 난투 피해 (최소 2 — 아무도 무해하지 않다) */
export function enemyBrawlDmgFor(cost: number): number {
  return Math.max(2, Math.round(cost * BRAWL_DMG_PER_COST));
}

// ---------------------------------------------------------------------------
// 웨이브 보상 — 웨이브젠이 goldReward = WAVE_GOLD_BASE + wave × WAVE_GOLD_PER_WAVE 로 계산
// (보스 오버라이드 웨이브 포함, 스테이지 데이터의 goldReward 필드는 무시된다)
// ---------------------------------------------------------------------------
export const WAVE_GOLD_BASE = 30;
export const WAVE_GOLD_PER_WAVE = 6;

// ---------------------------------------------------------------------------
// 살점 값 — bounty 를 '몫'으로 쪼개 **죽기 전에도** 지급한다 (sim/combat · sim/waves)
//
// 왜 필요한가 (실측): 지급 1회당 씹어야 하는 HP 가 스테이지1 w1 68 → w50 **1,545**,
// 스테이지6 w50 **67,426** 으로 벌어지는 동안 "지급 1회"라는 단위는 한 번도 안 변했다.
// 곧 trex 는 한 마리가 아니라 **잡몹 112마리분 HP 를 한 몸에 입은 것**인데 값은 한 번만
// 쳤다. 그 결과 스테이지1 기준선 봇 실측에서 판 시간의 **28.7%**(384초/판)가 10초 넘는
// 무수입 구간이었고, w50 최장 무수입은 **97.2초 = 웨이브 길이의 78.1%**였다
// (STRONG_BOT 도 95.9초 / 82.4% — 실력으로 못 지운다).
//
// 그래서 고친 것은 지급 **시점**이 아니라 지급 **단위의 정의**다: 스폰 시 그 개체가
// "이번 웨이브의 표준 사냥감 몇 마리분인가"(=몫 수 K)를 굳히고, bounty 를 K 로 나눠
// 몫 경계를 넘을 때마다 한 몫씩 낸다. **총액은 안 늘어난다** — 죽는 개체는 정확히
// bounty 를 받는다(마지막 몫은 사망 시에만 나가므로 처치가 여전히 결말이다).
// ---------------------------------------------------------------------------
/**
 * 한 몫의 최소 값어치(골드). 이 값 미만으로는 절대 안 쪼갠다.
 *
 * 4 = compy 의 bounty — "가장 싼 사냥감 한 마리 값"이 한 입의 하한이라는 뜻이다.
 * 이 하한이 **무한 모드의 상한 노릇도 한다**: bounty 는 웨이브에 따라 안 자라는데
 * (실측: trike 전 구간 정확히 36 · spino 240 · trex 480 고정, HP 만 1.06^n 으로 자란다)
 * `floor(bounty / 4)` 가 K 를 묶어 몫 수와 이벤트 수가 발산하지 못한다.
 *
 * 부수 효과로 **잡몹이 제 무리와 함께 나오는 한 무변경**이 된다 — compy(bounty 4) 는
 * 골드 상한만으로 K=1 이 확정이고, raptor(8)·blade(**16**)·archer(**21**) 의 골드 상한은
 * 각각 2·4·5 라 **덩치 상한이 1 일 때만**(= 그 웨이브의 중앙 HP와 비슷할 때) 무변경이다.
 * 실제 웨이브에서는 제 종끼리 무리로 나오므로 `round(maxHp/refHp)` = 1 이 되어 그 조건이
 * 거의 항상 성립한다 — 곧 결론("잡몹은 오늘과 같은 경로")은 맞지만 **근거는 덩치 쪽이다.**
 * (⚠ 이 주석은 "blade 15 · archer 20 … 덩치로도 골드로도 K 가 작다"고 적고 있었다.
 *  bounty 실측은 16·21 이고, 둘의 골드 상한은 1 이 아니다 — 언제 밀렸는지는 불명이다)
 */
export const BOUNTY_CHUNK_MIN_GOLD = 4;
/**
 * 한 개체가 낼 수 있는 최대 몫 수.
 *
 * 실측 유도: 스테이지1 w50 trex 의 최장 무수입이 **97.2초**였다. 97.2 ÷ 24 = **4.05초**로,
 * 잡몹 처치 중앙값(compy 3.4초)과 비보스 공백 p90(1.97초) 사이에 들어온다 — 곧 고HP
 * 개체를 씹는 동안의 수입 리듬이 잡몹 웨이브와 같은 크기가 된다. 24를 더 키우면 몫당
 * 골드가 20 밑으로 떨어져 배치 지가(창움막 T1 100골드)에 견줘 뜻이 없어진다.
 */
export const BOUNTY_CHUNK_MAX = 24;
/**
 * **생전 지급 몫** — 살아 있는 동안 받을 수 있는 bounty의 최대 비율(분자/분모).
 * 나머지는 사망 정산이 낸다. 총액은 **여전히 정확히 bounty**다(사망 시 잔액 전부).
 *
 * ⚠ **이 상수는 실측 스윕으로 유도됐다. 올리면 난이도 봉투가 빨개진다.**
 *
 * 스윕(스테이지1) — `autoplay.test.ts`의 **위험 5항목을 동시에** 측정했다.
 * `기준선`은 살점 값 적용 **전** 상태를 `git stash`로 되돌려 같은 시드로 잰 값이다:
 * ```
 *   생전몫  [1b]여유  [2]파괴  [3]minT  [7]지배  [12]입구요격
 *   ------  --------  -------  -------  -------  ------------
 *   기준선   54.9%     123      7        아님     52.5 ≤ 52.5   ← 변경 전 (문턱에 붙어 있다)
 *   1/1      59.5% ✗   113      7        아님     60.0 > 57.5 ✗
 *   3/4      55.2% ✗   119      5 ✗      아님     PASS
 *   2/3      54.3% ✓   103 ✓    7 ✓      기지자연  PASS         ← 채택
 *   1/2      52.8% ✓    89 ✗    6 ✗      기지자연  PASS
 *   1/4      51.9% ✓    88 ✗    7 ✓      아님     60.0 > 57.5 ✗
 * ```
 * ⚠ **어느 값도 5항목을 전부 통과시키지 못한다.** 봉투 여러 항목이 기준선에서 이미
 * 문턱에 정확히 붙어 있기 때문이다([3] 7 = 문턱 7 · [12] 52.5 = 문턱 52.5 ·
 * [1b] 54.9 대 55.0). 곧 이 봉투는 골드 곡선의 **어떤** 변화도 흡수하지 못한다.
 * ⚠⚠ **위 두 줄이 원래 이렇게 적혀 있었고, 그 진단은 폐기됐다** (정직성 정리 라운드):
 *   "2/3에서 남은 [7]은 **지표의 오독**이었고(마을 갈래의 여유가 baseHpMax에 오염된다),
 *    그 항목 주석이 미리 적어 둔 재유도 지침대로 두 번째 다리를 절대 누수 피해로
 *    바꿔서 해결했다 — 문턱 완화가 아니다."
 * **그 지표 교체는 그 다음 라운드가 되돌렸다.** 절대 누수 피해는 마을을 사는 전략과
 * 부호가 구조적으로 반대라(오래 버틸수록 흡수한 총량이 커진다) 9단계가 게임을 고쳐서
 * 껐던 **문서화된 지배 전략조차 독립 4블록 0/4로 못 잡는다** = 판별력이 0이다.
 * 두 번째 다리는 원래의 `slackAvg`로 돌아가 있고, [7]을 실제로 끈 것은 지표가 아니라
 * **마을 비용 표**다(600/900/1300/1700 → 900/1000/1200/1400, 누적 4,500 불변 —
 * 유도 전문은 `src/data/hometown.ts`의 '살점 값 단계' 절).
 * 곧 이 스윕표의 `[7]` 열은 **그때 쓰던 지표로 읽은 값**이고, 지금 지표로 다시 읽으면
 * 2/3 · 옛 비용표에서 [7]은 blk1000이 적색이었다. 되돌리기 실측 전문은
 * `autoplay.test.ts`의 7번 항목 주석에 있다.
 *
 * 원인은 총량이 아니라 **시점**이다:
 * STRONG_BOT의 총수입 증가는 **+1.5%**뿐인데 그 돈이 **보스 웨이브 도중**에 들어와
 * 대공 타워가 한 박자 일찍 서고, 그러면 하늘길(w22·26·34·38·42·46)이 기지 체력을
 * 못 가져간다(종료 HP에 22~25가 대량 등장). 곧 이 봉투는 골드의 **양이 아니라
 * 도착 시각**에 민감하다.
 *
 * 그래서 문턱을 내리는 대신 **게임 쪽을 고쳤다**: 생전 지급을 깎아 조기 수입 우위만
 * 정확히 겨냥한다. 지급 **횟수**는 그대로다 — 한 몫의 최솟값이 `MIN_GOLD × 2/3` = 2.67
 * 골드라 1 미만으로 내려가는 몫이 없고, 곧 몫 경계가 하나도 안 사라진다.
 * 이 변경이 고치려던 "무수입 구간의 길이"는
 * 손대지 않으면서 봉투만 되돌린다. `BOUNTY_CHUNK_MAX`를 줄이는 손잡이보다 나은
 * 이유가 이것이다 — 그쪽은 공백 단축 효과를 직접 반납한다.
 *
 * 1/3이 1/2보다 **더 나쁜 것**에 주의하라(입구요격이 깨진다). 이 지형은 단조롭지 않다 —
 * 봇이 문턱을 넘는 시점이 이산적이라 돈이 줄어도 결과가 뒤집힐 수 있다.
 * 값을 바꾸려면 반드시 위 두 항목을 **함께** 다시 재라.
 *
 * 부수 효과로 **"죽여야 제값을 받는다"가 세진다** — 원시시대 사냥의 뜻과도 맞고,
 * 딜만 넣고 못 죽이는 배치(입구 요격)가 받던 이득을 정확히 그만큼 깎는다.
 */
export const BOUNTY_CHUNK_LIVE_NUM = 2;
export const BOUNTY_CHUNK_LIVE_DEN = 3;

/**
 * 몫 수 K — 이 개체가 **이번 웨이브의 표준 사냥감 몇 마리분인가**. 스폰 시 한 번 굳는다.
 *
 * 세 상한의 min 이고, 각각 다른 것을 막는다:
 *  · `round(maxHp / refHp)` — 덩치. 이게 본체다. 잡몹이면 1이 되어 **오늘과 같은 동작**.
 *  · `floor(bounty / BOUNTY_CHUNK_MIN_GOLD)` — 한 몫이 4골드 밑으로 안 내려간다.
 *  · `BOUNTY_CHUNK_MAX` — 이벤트 수와 "한 입의 체감"의 상한.
 *
 * **무한 모드(HP 1.06^n)에 완전히 불변이다**: maxHp 도 refHp 도 같은 extraHpMul 을 지고
 * 있어 분자·분모가 함께 커진다. 곧 개체당 지급 **횟수**가 웨이브와 무관하게 고정이고,
 * 수입의 리듬이 무한 모드에서도 유지된다. (남는 것: 몫 하나가 씹어야 하는 HP 는 계속
 * 커진다 — b/HP 가 w1 0.1176 → w50 0.0175 로 떨어지는 기울기는 **일부러** 안 고친다.
 * 이 변경의 렌즈는 "언제 주는가"이지 "얼마를 주는가"가 아니고, 후자는 총량 보존을
 * 정면으로 깨는 별개 결정이다.)
 */
export function bountyChunksFor(maxHp: number, bounty: number, refHp: number): number {
  const byBulk = Math.round(maxHp / Math.max(1, refHp));
  const byGold = Math.floor(bounty / BOUNTY_CHUNK_MIN_GOLD);
  return Math.max(1, Math.min(byBulk, byGold, BOUNTY_CHUNK_MAX));
}

// ---------------------------------------------------------------------------
// 웨이브젠 형태 제약 — 성능 예산(인스턴싱/풀)과 난이도 곡선 안정화용
// ---------------------------------------------------------------------------
/** 한 SpawnGroup의 최대 마릿수 */
export const GROUP_MAX_COUNT = 25;
/** 한 웨이브의 최대 총 스폰 수 (초과 예산은 hpMul 보정으로 흡수) */
export const WAVE_MAX_SPAWNS = 60;
/** elite 템플릿: 마릿수를 줄이는 대신 개체 HP 배율 */
export const ELITE_HP_BONUS = 1.8;
/**
 * 총 HP 정규화 보정 한계 — 웨이브 총 HP를 예산 곡선(budget × 평균 hp/cost × hpMul)에
 * 맞추기 위한 그룹 hpMul 보정 배율의 클램프. 상한 40은 endless 후반 스폰 캡 흡수용.
 */
export const HP_CORR_MIN = 0.25;
export const HP_CORR_MAX = 40;

// ---------------------------------------------------------------------------
// 메타 진행 — 스테이지/조각 보상 규칙 (meta 트랙 참조용)
// ---------------------------------------------------------------------------
/** 보스 처치 시 타워 조각 드랍 수 (결과 화면 정산) */
export const SHARDS_PER_BOSS_KILL = 2;
/** 무한 모드 해금 조건: 스테이지 3 클리어 */
export const ENDLESS_UNLOCK_STAGE = 3;

// ---------------------------------------------------------------------------
// 상성 계량 — 특성 태그 · 유효 배율 · 수요 막대 (docs/counter-plan.md 1단계)
//
// **여기는 밸런스를 정하지 않는다. 이미 있는 수치를 읽어 화면이 읽을 수 있는 모양으로
// 바꿀 뿐이다.** 이 절의 어떤 함수도 시뮬레이션에서 호출되지 않는다 —
// sim은 여전히 combat.damageEnemy 한 곳에서만 피해를 계산하고, 아래 식은 그 계산의
// **거울**일 뿐이다. 둘이 어긋나면 화면이 거짓말을 하는 것이므로,
// tests/data/preview.test.ts가 두 식이 같은 답을 내는지 잠근다.
// ---------------------------------------------------------------------------

/**
 * 배지 우선순위 — 칩에는 **하나만** 그린다(390px에서 둘은 안 읽힌다).
 * 순서의 근거는 "무엇이 내 답을 가장 크게 바꾸는가"다:
 *  air    하드 게이트. 대공이 없으면 배율이 0이라 다른 무엇도 의미가 없다.
 *  shield 앞의 N타를 통째로 버린다 — 타워 선택이 아니라 발사 횟수를 바꾼다.
 *  armor  타격 크기를 벌한다 — 연사/한 방의 서열을 뒤집는다.
 *  heal   처치 순서를 바꾼다(먼저 잡을 것).
 *  raid   내 타워가 표적이 된다 — 배치 거리를 바꾼다.
 *  enrage 마지막 40%만 빨라진다 — 답을 바꾸지는 않는다.
 */
export const TRAIT_PRIORITY: readonly TraitTag[] = [
  'air', 'shield', 'armor', 'hide', 'splash', 'heal', 'raid', 'enrage',
];

/**
 * 적의 특성 태그. 2단계에서 가죽🟫·흩어짐〽 두 분기가 들어왔다(정화✧는 6단계).
 * **I-1(배지 하나)이 armor/hide/splashResist/shieldHits의 동시 보유를 금지**하므로
 * 앞의 넷 중 실제로 켜지는 것은 언제나 최대 하나다 — 우선순위는 그 뒤(heal/raid/enrage)와
 * 겹칠 때만 일한다.
 */
export function enemyTraitsOf(def: EnemyDef): TraitTag[] {
  const out: TraitTag[] = [];
  if (def.flying) out.push('air');
  if ((def.shieldHits ?? 0) > 0) out.push('shield');
  if (def.armor > 0) out.push('armor');
  if (def.hide !== undefined) out.push('hide');
  if (def.splashResist !== undefined) out.push('splash');
  if (def.healAura) out.push('heal');
  if (def.towerAttack) out.push('raid');
  if (def.enrage) out.push('enrage');
  // 우선순위 정렬 — [0]이 칩에 그릴 배지 하나다
  out.sort((a, b) => TRAIT_PRIORITY.indexOf(a) - TRAIT_PRIORITY.indexOf(b));
  return out;
}

/**
 * 이 타워가 **때릴 수 있는 무언가**를 가졌는가. 전쟁북(drum)은 dmg 0에 지상·공중
 * 둘 다 false라 수요 막대의 분모가 정의되지 않는다 — 0으로 그리면 "쓸모없다"는
 * 거짓말이 되므로 아예 그리지 않는다.
 */
export function isAttackTower(def: TowerDef): boolean {
  if (!def.canTargetGround && !def.canTargetAir) return false;
  return def.tiers.some((t) => t.dmg > 0);
}

/**
 * `enemyDamaged.mitigated`가 실리는 **최소 감산 비율**. 원래 피해의 이만큼을 못 넣었을
 * 때만 "무엇이 깎았다"가 이벤트에 실린다.
 *
 * 왜 문턱이 필요한가 — 가죽 상한은 타워 dmg 바로 위/아래를 스칠 수 있다(창 T3 28 대
 * boar w1 cap 27 = 1 손실). 그 1을 부호로 그리면 괄호가 **매 타격 켜져** 있어 정보가
 * 아니라 배경이 된다. 이 저장소가 `damagenumbers.ts`에서 이미 같은 판단을 했고
 * (거기 `MITIGATED_SHARE`와 같은 값이다), 그 잣대를 sim 쪽으로 옮겨 세 축이 **하나의
 * 규칙**을 쓰게 한다. 결정론이라 sim에 있어도 무해하다(순수 산술).
 */
export const MITIGATED_MIN_SHARE = 0.25;

/**
 * **가죽 상한의 유일한 식** — `sim/combat.damageEnemy`와 `sim/battle.previewWave`가
 * 둘 다 이것을 부른다. 거울을 두 번 적으면 언젠가 갈라지므로 식은 한 곳에만 둔다.
 * 최소 1인 이유: 상한이 0이 되면 어떤 타워도 그 적을 **영원히** 못 죽인다.
 */
export function hideCapFor(maxHp: number, hide: number): number {
  return Math.max(1, Math.round(maxHp * hide));
}

/**
 * 이 타워의 피해가 **폭발로** 들어가는가 — 곧 흩어짐〽이 걸리는가.
 *
 * `attackKind: 'ballistic'` + `splash`면 `impactBallistic`이 **직격 없이** `applyArea`만
 * 부르고(`excludeId = -1`), `splashScale(0)` = 1이라 착탄점의 적도 **전량을 폭발로** 받는다.
 * 곧 이 타워는 피해의 100%가 splash 플래그를 달고 damageEnemy에 들어간다.
 * 지금 이 조건에 걸리는 타워는 **투석기 하나뿐**이고(유일한 ballistic+splash),
 * 그래서 이 축의 배율이 근사가 아니라 **정확**하다.
 */
export function isSplashDamage(def: TowerDef): boolean {
  return def.attackKind === 'ballistic' && def.tiers.some((t) => t.splash !== undefined);
}

/**
 * 타워 한 종이 적 한 종에게 내는 **유효 배율** (0~1).
 *   못 때린다(대공 없음 등) → 0
 *   때린다                  → `damageEnemy` 한 방을 그대로 되짚은 값 / dmg
 *
 * **combat.damageEnemy의 거울이다** — 순서까지 같다:
 *   흩어짐(폭발 한정 비율) → 장갑(고정 감산, 최소 1) → 가죽(타격당 상한)
 * 둘이 어긋나면 미리보기 막대와 카드 경고가 조용히 거짓말을 한다.
 * `tests/data/counter.test.ts`가 두 식이 같은 답을 내는지 잠근다.
 *
 * 남은 오차를 정직하게 적는다: 독가시의 DoT는 armor를 무시하고(ignoreArmor), 번개의 체인은
 * 여러 마리를 한 번에 친다. 곧 이 값은 **한 방의 비율**이지 DPS 비율이 아니다.
 * 가죽이 특히 그렇다 — cap은 **대상별**이라 여러 마리를 덮는 폭발에서는 마리마다 따로
 * 걸리고, 이 함수는 그중 한 마리를 본다.
 */
export function towerEffVs(def: TowerDef, tier: number, e: WavePreviewEntry): number {
  const canHit = e.flying ? def.canTargetAir : def.canTargetGround;
  if (!canHit) return 0;
  const t = def.tiers[Math.max(0, Math.min(def.tiers.length - 1, Math.floor(tier)))];
  const dmg = t ? t.dmg : 0;
  if (dmg <= 0) return 0;
  const resist = isSplashDamage(def) ? (e.splashResist ?? 0) : 0;
  const raw = resist > 0 ? dmg * (1 - resist) : dmg;
  let dealt = Math.max(1, raw - e.armor);
  if (e.hideCap !== undefined) dealt = Math.min(dealt, e.hideCap);
  return dealt / dmg;
}

/**
 * **수요 막대** — demand[t] = Σ(hp_i × eff(t,i)) / Σ hp_i.
 * 못 때리는 것은 0으로 세므로 막대 하나가 상성과 대공을 **함께** 말한다.
 * 총 HP가 0이면(빈 웨이브) 0을 낸다.
 */
export function demandFor(def: TowerDef, tier: number, entries: readonly WavePreviewEntry[]): number {
  let num = 0;
  let den = 0;
  for (const e of entries) {
    den += e.totalHp;
    num += e.totalHp * towerEffVs(def, tier, e);
  }
  return den > 0 ? num / den : 0;
}

/**
 * 무력 판정선 — 이 아래면 손패 카드에 회색 오버레이가 붙는다.
 * **장갑 축 전용이다** (공중은 아래 AIR_BLIND_SHARE가 따로 맡는다).
 *
 * 0.72의 유도 — 스테이지1 계량기 실측(previewWave(1..50), 덱 spear/catapult/frost, T1):
 *   장갑이 두꺼운 웨이브 11개(w10·20·28·30·32·33·37·39·40·44·45, armor 비중 53~82%)
 *     얼음 T1(dmg 7)  0.53~0.70  ← **전부 잡힌다**
 *     창   T1(dmg 12) 0.73~0.82  ← 안 잡힌다 (아프지만 무력하지는 않다)
 *     투석기 T1(dmg 30) 0.89~0.93 ← 안 잡힌다
 *   w50(trex armor 8이 85%): 창 T1 **0.43** · 얼음 0.27 → 둘 다 잡힌다
 *   장갑이 없는 웨이브 30개: 전 카드 1.00 → **한 장도 회색이 되지 않는다**
 * 곧 이 선은 "장갑 앞의 저티어 얼음"과 "보스 앞의 저티어 창"만 고른다.
 * 그리고 **티어를 올리면 경고가 사라진다** — 얼음 T3(dmg 18)은 같은 웨이브에서 0.78이다.
 * (채널이 매 웨이브 켜져 있으면 그건 정보가 아니라 배경이다)
 */
export const DEMAND_WEAK = 0.72;
/**
 * **대공 맹점** — 공중을 못 때리는 타워는, 이번 웨이브의 공중 HP 비중이 이 값 이상이면
 * 수요 막대와 **무관하게** 회색이 된다.
 *
 * 왜 판정선을 따로 두는가: 대공은 연속 감산이 아니라 **하드 게이트**다. 한 대도 못 댄다.
 * 그런데 스테이지1의 하늘길은 공중 비중이 크지 않아(w22 23% · w26 16% · w34 11% ·
 * w38 22% · w42 11% · w46 5%) 투석기의 수요 막대가 0.77~0.95에 머문다 — 곧 위 선
 * 하나로는 **첫 하늘길에서 대공이 없는 손이 경고를 못 받는다.** 게이트에는 게이트의 자가
 * 있어야 한다. 0.10은 w46(5%, 27마리 중 프테라 둘)만 빼고 다섯 하늘길을 전부 잡는다 —
 * "스무 마리 중 두 마리를 못 때린다"는 카드를 죽이지 않지만 "넷 중 하나"는 죽인다.
 */
export const AIR_BLIND_SHARE = 0.1;
/**
 * 유리 판정선 — 이 위면 옅은 테두리. 단, **이번 웨이브에 벌하는 축이 실제로 있을 때만**
 * (아래 hasCounterAxis). 아무 축도 없는 웨이브에서는 전 카드가 1.00이라
 * 테두리가 전부 켜지고, 그러면 테두리는 아무 말도 하지 않는다.
 *
 * ⚠ 정직하게: 이 선은 **절대값**이라, 장갑 웨이브에서는 아무도 테두리를 못 받는다
 * (T1 최고가 투석기 0.93이다). 그건 사실 그대로다 — 장갑 앞에서 "유리한" 카드는 없고
 * **덜 나쁜** 카드가 있을 뿐이며, 그 서열은 수요 막대가 이미 길이로 말한다.
 * 상대 순위로 테두리를 켜면 "이 셋 중 제일 낫다"가 "이건 잘 듣는다"로 읽혀,
 * 세 장 다 나쁜 손에서 플레이어를 오도한다.
 */
export const DEMAND_STRONG = 0.97;
/**
 * 이번 웨이브에 **상성 축이 있는가** — 장갑이 있거나 공중이 섞였고, 그 몫이
 * 총 HP의 이 비율 이상인가. 미만이면 "그냥 평범한 웨이브"라 유리 표시를 켜지 않는다.
 */
export const COUNTER_AXIS_SHARE = 0.15;

export function hasCounterAxis(entries: readonly WavePreviewEntry[]): boolean {
  let axis = 0;
  let total = 0;
  for (const e of entries) {
    total += e.totalHp;
    if (
      e.flying ||
      e.armor > 0 ||
      e.hideCap !== undefined ||
      e.splashResist !== undefined ||
      e.traits.includes('shield')
    ) {
      axis += e.totalHp;
    }
  }
  return total > 0 && axis / total >= COUNTER_AXIS_SHARE;
}

/** 이번 웨이브의 **공중 HP 비중** (0~1) */
export function airShareOf(entries: readonly WavePreviewEntry[]): number {
  let air = 0;
  let total = 0;
  for (const e of entries) {
    total += e.totalHp;
    if (e.flying) air += e.totalHp;
  }
  return total > 0 ? air / total : 0;
}

/**
 * **이 카드가 이번 웨이브에서 무엇에 막히는가** — 막히지 않으면 null.
 * UI(회색 오버레이 + 특성 아이콘)와 테스트가 같은 함수를 쓴다.
 *
 * 두 축을 **다른 자로** 잰다 (위 두 상수의 주석 참조):
 *  1. 공중 — 못 때리는데 공중 비중이 AIR_BLIND_SHARE 이상이면 즉시 'air'.
 *     하드 게이트라 평균으로 희석되면 안 된다.
 *  2. 나머지 — 수요 막대가 DEMAND_WEAK 아래면, **가장 많이 깎은 축**의 이름을 돌려준다.
 * 둘 다 걸리면 공중이 이긴다. 한 대도 못 대는 쪽이 언제나 더 큰 문제다.
 *
 * 2단계에서 축이 셋(장갑·가죽·흩어짐)이 됐으므로 "무엇에 막혔는가"를 **체력 가중으로 귀속**
 * 한다 — `damageEnemy`가 `mitigated` 한 필드를 고르는 규칙과 같은 잣대다(가장 크게 깎은 것 하나).
 */
export function counteredBy(
  def: TowerDef,
  tier: number,
  entries: readonly WavePreviewEntry[],
): TraitTag | null {
  if (!isAttackTower(def)) return null;
  if (!def.canTargetAir && airShareOf(entries) >= AIR_BLIND_SHARE) return 'air';
  if (demandFor(def, tier, entries) >= DEMAND_WEAK) return null;
  // 축별로 "이 축이 없었다면 얼마나 더 들어갔을까"를 체력 가중으로 모은다
  const loss: Partial<Record<TraitTag, number>> = {};
  let total = 0;
  for (const e of entries) {
    const canHit = e.flying ? def.canTargetAir : def.canTargetGround;
    if (!canHit) continue;
    const gap = e.totalHp * (1 - towerEffVs(def, tier, e));
    if (gap <= 0) continue;
    total += gap;
    // 이 종에서 실제로 켜져 있는 축 하나에 귀속한다 (I-1이 둘 이상을 금지한다)
    const tag: TraitTag =
      e.hideCap !== undefined
        ? 'hide'
        : e.splashResist !== undefined && isSplashDamage(def)
          ? 'splash'
          : 'armor';
    loss[tag] = (loss[tag] ?? 0) + gap;
  }
  if (total <= 0) return null;
  let best: TraitTag = 'armor';
  let bestV = -1;
  for (const [k, v] of Object.entries(loss) as [TraitTag, number][]) {
    if (v > bestV) {
      best = k;
      bestV = v;
    }
  }
  return best;
}

/** 이 카드가 이번 웨이브에 **유리한가** (옅은 테두리) */
export function favoredAgainst(
  def: TowerDef,
  tier: number,
  entries: readonly WavePreviewEntry[],
): boolean {
  if (!isAttackTower(def)) return false;
  if (!hasCounterAxis(entries)) return false;
  return demandFor(def, tier, entries) >= DEMAND_STRONG;
}
