/**
 * 스테이지 5 — 늪지 (swamp). 가장 긴 사행 경로(~36타일)지만 물(~)이 사방을 침식해
 * 슬롯 위치가 까다롭다. 전 적군 등장(골렘 제외), 40웨이브에 이른 트렉스 기습.
 */
import type { StageDef } from '../types';
import { bossWave, g, v } from './helpers';

export const stage05: StageDef = {
  id: 5,
  nameKey: 'stage.5.name',
  biome: 'swamp',
  gridW: 13,
  gridH: 17,
  layout: [
    '~~.........~~',
    '~~......~~.~~',
    '~~...o..~~.~~',
    '...........~~',
    '...o.......~~',
    '.o...........',
    '...o....o....',
    '.............',
    '.....o.......',
    '~~.........o.',
    '~~.......o...',
    '~~...........',
    '~~....o.....~',
    '~~.o........~',
    '~~..........~',
    '~~.....o....~',
    '~~....#.....~',
  ],
  paths: [
    [v(6, 0), v(6, 3), v(2, 3), v(2, 7), v(10, 7), v(10, 11), v(4, 11), v(4, 14), v(8, 14)],
  ],
  /*
   * ── 하늘길 — **네 접힘을 가로로 지른 뒤 세로로 내려온다** ────────────────────
   * 지상은 (6,0)에서 좌우로 네 번 접히며 내려온다. 하늘길은 서쪽 변 (0,4)에서 동쪽으로
   * 질러 (8,4)에서 남하해 화덕으로. 길이 18.00 = 지상(36.00)의 **0.500**.
   * 지상 스폰이 북쪽 변이라 **다른 변**에서 들어온다. 채점: 덮임 1.01 · 겹침 0.19 — 1위(0.887).
   */
  airPaths: [[v(0, 4), v(8, 4), v(8, 14)]],
  baseCell: v(8, 14),
  baseHp: 30,
  startGold: 280,
  waveCount: 50,
  wavePlan: {
    budgetBase: 39,
    budgetGrowth: 1.135,
    hpBase: 1.9,
    hpGrowth: 1.055,
    seed: 5077,
    // 습격대 4종(스테이지3 완성) 유지 — 여기서는 매머드·힐러와 겹쳐 밀도가 올라간다
    allowedEnemies: [
      'raptor', 'compy', 'boar', 'trike', 'ptera', 'warrior', 'shaman', 'ankylo', 'mammoth',
      'blade', 'lancer', 'archer', 'hexer',
    ],
    /*
     * ── 공중 축 게이트 (2라운드 2-a) ────────────────────────────────────────
     * 이 셋이 없을 때 실제로 벌어지던 일(실측, wavegen 을 직접 돌려 w1~50 을 뽑았다):
     *   · `isAirWave` 가 첫 조건에서 무조건 false → **보장된 하늘길 웨이브가 0개**
     *   · `allowed` 가 매 웨이브 전체 풀 → `genMixed` 가 아무 웨이브에서나 익룡을 뽑는다
     *     (s5 는 w4, s6 은 w3 부터 나왔다 — 둘 다 air_raid 가 아니라 mixed 였다)
     *   · `airLeft` 가 Infinity → 한 그룹이 GROUP_MAX_COUNT(25)까지 간다
     *     (s2 w34/42/46 · s3 w33/37/38/44 · s5 w29~47 이 **익룡 25마리** 웨이브였다)
     * 곧 "공중이 없다"가 아니라 **통제되지 않은 채 너무 일찍·너무 많이** 있었다.
     *
     * 값의 근거: 스테이지가 뒤로 갈수록 하늘이 **일찍 열리고 무리가 커진다**
     * (s1 22/2 → s2 18/3 → s3 16/3 → s4 14/4 → s5 12/4 → s6 10/5).
     * ⚠ **이 두 숫자는 밸런스 손잡이다** — 사용자가 돌린다(CLAUDE.md「밸런스는 …」).
     *   `curvePool: 'all'` 이 있는 한 이 값을 어떻게 돌려도 **HP 곡선은 안 움직인다**
     *   (게이트 유무만 곡선을 움직이고 값은 무관하다는 것이 실측이다).
     */
    /*
     * ⚠ **s5 은 게이트(`airFromWave`)를 걸지 않고 마릿수 상한만 건다.** 이유는 실측이다.
     *
     * 게이트를 걸면 w1~게이트 구간에서 익룡이 추첨 풀에서 빠지고, 그 예산이 지상으로
     * 재분배되면서 **초반 웨이브 구성이 바뀐다**. 기준선 봇은 s5·s6 에서 6웨이브에
     * 죽으므로 익룡을 한 마리도 못 보는데, 바로 그 초반 구성 변화 때문에
     * `tests/sim/ladder.test.ts` 의 판당 골드 단계(≤0.95)가 깨진다:
     *   s6/s5 골드비 — 배포본 0.8631 → 게이트 후 **0.9514**
     *   시드 집합 다섯(1000·2000·3000·4000·5000)에서 배포본은 0.8631~0.9163 로 전부 통과,
     *   게이트 후는 0.9514~0.9912 로 **전부 실패** — 잡음이 아니라 계통적 +0.08 이동이다.
     * 그리고 `airFromWave` 값으로는 못 고친다(8·10·14·18·22 전부 같은 값이 나온다 —
     * 봇이 그 웨이브에 닿지 못하기 때문이다). s6 예산을 올리는 것도 **계단이라 안 듣는다**
     * (×1.03 → 0.9666 · ×1.10 → 0.9671 로 오히려 나빠지고 ×1.20 에서야 0.7674 로 떨어진다).
     *
     * 상한만 걸면 다섯 시드 전부 최악 0.8589~0.9188 로 통과하고, 정작 병리적이던 것
     * — `airLeft` 가 Infinity 라 한 그룹이 `GROUP_MAX_COUNT`(25)까지 가던 것 — 은 사라진다.
     * (s5 는 w29~w47 이 전부 익룡 25마리 웨이브였다. 상한 뒤 한 웨이브 최대 4마리)
     *
     * ⚠ 남은 문제: **첫 익룡이 여전히 w4 로 이르다**(s1 은 w22). 그것을 늦추는 것은
     *   위 실측대로 사다리를 건드리므로 **밸런스 결정이라 사용자 몫으로 남긴다**
     *   (CLAUDE.md「밸런스는 사용자가 직접 한다」). 켜려면 아래 한 줄을 살리면 되고,
     *   `curvePool: 'all'` 이 이미 있어 HP 곡선은 그때도 안 움직인다.
     *   // airFromWave: 12,
     */
    airMaxCount: 4,
    // 곡선은 지금 그대로 못 박는다 — 근거는 types.ts 의 curvePool 주석.
    curvePool: 'all',
    bossOverrides: {
      // w10 첫 보스 벽 완화: 전체 ×0.65 (w9 대비 총 HP ~5.9배 → ~3.8배)
      10: bossWave(
        g('spino', 1, 0, 30, 0.59),
        g('shaman', 3, 45, 120, 0.65),
        g('boar', 8, 15, 180, 0.65),
      ),
      20: bossWave(g('spino', 1, 0, 30, 1.15), g('mammoth', 2, 90, 120), g('shaman', 3, 45, 240)),
      // 클라이맥스 보정: 직전 웨이브 대비 총 HP ≥1.15×
      30: bossWave(
        g('spino', 2, 240, 30, 1.56),
        g('ankylo', 4, 50, 150, 1.2),
        g('shaman', 3, 45, 300, 1.2),
      ),
      40: bossWave(
        g('trex', 1, 0, 30, 1.56),
        g('mammoth', 3, 90, 240, 1.2),
        g('shaman', 4, 45, 180, 1.2),
        g('warrior', 8, 20, 120, 1.2),
      ),
      50: bossWave(
        g('trex', 1, 0, 30, 1.5),
        g('spino', 1, 0, 420, 1.15),
        g('mammoth', 2, 90, 240, 1.15),
      ),
    },
  },
  firstClearAmber: 320,
  perWaveAmber: 3,
  // 2-c — 재충전형 방패(warrior)의 전용 답. 방패 자체는 s2 부터 있지만 이 타워는
  //        '발사 간격'이라는 축 하나에 특화돼 있어 다른 답을 다 써 본 뒤에 준다.
  unlockTowers: ['rattletrap'],
};
