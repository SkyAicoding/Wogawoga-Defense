/**
 * 스테이지 2 — 밀림 (jungle). 지그재그 단일 경로 + 첫 공중 유닛(ptera) 등장.
 * 경로: 우상단 스폰 → 좌/우로 세 번 꺾여 좌하단 화덕. 모서리 연못(~)이 배치 공간을 조인다.
 */
import type { StageDef } from '../types';
import { bossWave, g, v } from './helpers';

export const stage02: StageDef = {
  id: 2,
  nameKey: 'stage.2.name',
  biome: 'jungle',
  gridW: 11,
  gridH: 16,
  layout: [
    '~~.........',
    '~~.........',
    '..........o',
    '.....o..o..',
    '...........',
    '..o........',
    '....o......',
    '......o....',
    '...........',
    '.....o..o..',
    '...........',
    '....o......',
    '...........',
    'o........~~',
    '.........~~',
    '....#....~~',
  ],
  paths: [
    [v(9, 0), v(9, 4), v(3, 4), v(3, 8), v(7, 8), v(7, 12), v(1, 12), v(1, 14)],
  ],
  /*
   * ── 하늘길 — **S자 세 접힘을 세로로 관통한다** (스테이지1과 같은 원리) ─────────
   * (2,0)에서 남하해 (2,14)에서 동쪽으로 한 번 꺾어 화덕으로 들어간다.
   * 길이 15.00 = 지상(30.00)의 **0.500**. 지상 스폰이 (9,0)이라 **반대쪽 변**에서 온다.
   * 채점: 덮임 1.09(지상 대비) · 지상 겹침 0.10 — 44개 후보 중 1위(점수 1.028, 2위 0.896).
   */
  airPaths: [[v(2, 0), v(2, 14), v(1, 14)]],
  baseCell: v(1, 14),
  baseHp: 20,
  startGold: 240,
  waveCount: 50,
  wavePlan: {
    budgetBase: 24,
    budgetGrowth: 1.12,
    hpBase: 1.2,
    hpGrowth: 1.048,
    seed: 2027,
    // 습격대 데뷔 2단계 — 큰창잡이(lancer) 합류. 사거리 2.8이라 투창병(2.4)이 못 닿던
    // 자리까지 던지고, 정지 상한 90틱으로 전 종 최장이라 한 자리에서 제일 오래 쏜다.
    // 경로 이격이 '한 칸으로는 부족하다'를 배우는 스테이지.
    allowedEnemies: [
      'raptor', 'compy', 'boar', 'trike', 'ptera', 'warrior', 'blade', 'archer', 'lancer',
    ],
    /*
     * ── 공중 축 게이트 (2라운드 2-a) ────────────────────────────────────────
     * 이 셋이 없을 때 실제로 벌어지던 일(실측, wavegen 을 직접 돌려 w1~50 을 뽑았다):
     *   · `isAirWave` 가 첫 조건에서 무조건 false → **보장된 하늘길 웨이브가 0개**
     *   · `allowed` 가 매 웨이브 전체 풀 → `genMixed` 가 아무 웨이브에서나 익룡을 뽑는다
     *     (s5 는 w4, s6 은 w3 부터 나왔다 — 둘 다 air_raid 가 아니라 mixed 였다)
     *   · `airLeft` 가 Infinity → 한 그룹이 GROUP_MAX_COUNT(25)까지 간다
     *     (s2 w34/42/46 · s3 w33/37/38/44 · s5 w29~47 이 **익룡 25마리** 웨이브였다)
     * 곧 "공중이 없다"가 아니라 **통제되지 않은 채 너무 일찍·너무 많이** 있었다.
     *
     * 값의 근거: 스테이지가 뒤로 갈수록 하늘이 **일찍 열리고 무리가 커진다**
     * (s1 22/2 → s2 18/3 → s3 16/3 → s4 14/4 → s5 12/4 → s6 10/5).
     * ⚠ **이 두 숫자는 밸런스 손잡이다** — 사용자가 돌린다(CLAUDE.md「밸런스는 …」).
     *   `curvePool: 'all'` 이 있는 한 이 값을 어떻게 돌려도 **HP 곡선은 안 움직인다**
     *   (게이트 유무만 곡선을 움직이고 값은 무관하다는 것이 실측이다).
     */
    airFromWave: 18,
    airMaxCount: 3,
    // 곡선은 지금 그대로 못 박는다 — 근거는 types.ts 의 curvePool 주석.
    curvePool: 'all',
    bossOverrides: {
      // w10 첫 보스 벽 완화: 전체 ×0.55 (w9 대비 총 HP ~7.3배 → ~4.0배)
      10: bossWave(g('spino', 1, 0, 30, 0.41), g('warrior', 5, 25, 150, 0.55)),
      20: bossWave(g('spino', 1, 0, 30, 1.05), g('ptera', 6, 20, 120), g('boar', 6, 18, 210)),
      30: bossWave(g('spino', 2, 240, 30, 1.0), g('warrior', 6, 25, 150)),
      // 습격대 종을 hp/cost 기준에서 뺀 뒤(wavegen.refHpPerCost) 일반 웨이브 총 HP가
      // 올라가 w40 보스가 w39에 1.10배 미달(1.098)이 됐다 — 클라이맥스 복원용 상향.
      40: bossWave(
        g('spino', 2, 240, 30, 1.42),
        g('ptera', 8, 18, 120, 1.1),
        g('trike', 4, 50, 300, 1.1),
      ),
      // 클라이맥스 보정: w49 대비 총 HP ≥1.15× (w40과 같은 이유로 재상향)
      50: bossWave(g('trex', 1, 0, 30, 1.5), g('raptor', 10, 15, 240, 1.25)),
    },
  },
  firstClearAmber: 160,
  perWaveAmber: 1,
  unlockTowers: ['poison'],
};
