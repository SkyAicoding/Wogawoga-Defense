/**
 * 스테이지 1 — 초원 (grassland). 초심자용: 단일 S자 경로(길이 ~37타일)로 화력 집중이 쉽다.
 * 경로: 좌상단 스폰 → 좌우로 세 번 접히며 하강 → 우하단 화덕.
 */
import type { StageDef } from '../types';
import { bossWave, g, v } from './helpers';

export const stage01: StageDef = {
  id: 1,
  nameKey: 'stage.1.name',
  biome: 'grassland',
  gridW: 11,
  gridH: 15,
  // '.' 지상  'o' 슬롯  '#' 장식 (경로 셀은 paths에서 래스터라이즈)
  layout: [
    '..........#',
    '...........',
    'o.o........',
    '...........',
    '...o..o....',
    '........o..',
    '.....o.....',
    '...........',
    '.....o.....',
    'o..........',
    '.....o.....',
    '...........',
    '........o..',
    '...........',
    '#.........#',
  ],
  paths: [
    [v(1, 0), v(1, 3), v(9, 3), v(9, 7), v(1, 7), v(1, 11), v(9, 11), v(9, 13)],
  ],
  baseCell: v(9, 13),
  baseHp: 25,
  startGold: 300,
  waveCount: 50,
  wavePlan: {
    budgetBase: 20,
    budgetGrowth: 1.1,
    hpBase: 1.0,
    /**
     * 1.020 유지 (4단계에서 1.024 → 1.020 원복).
     *
     * 3단계는 "습격대가 난이도를 더하는 대신 누수 압박과 맞바꾸고 있다"는 실측을 근거로
     * 1.020 → 1.024로 올렸다. 그 실측이 **경제 버그에 오염돼 있었다**:
     * 당시 raid 편성은 최소 인원 보장(wavegen RAID_MIN_*)으로 예산보다 많은 마릿수를
     * 뽑으면서 보상만 마릿수에 비례해 부풀어(스테이지1 w1~50 총수입 +18.7%),
     * 습격대를 넣으면 오히려 게임이 쉬워지고 있었다. 그 상태에서 "습격대가 약하다"고
     * 읽고 hpGrowth로 보정한 것이므로, 보상 상한(wavegen.capBounty)을 넣은 지금은
     * 근거가 사라졌다.
     *
     * 보상 상한 이후 재실측(시드 20개, 덱 spear+catapult+frost, 별 0):
     *   hpGrowth 1.020 → 습격대O 15/20승·기지HP합 171 / 습격대X 16/20승·156
     *   hpGrowth 1.024 → 습격대O 13/20승·106      / 습격대X 13/20승·106
     * 1.024는 습격대 유무의 차이를 없애면서 초심자 스테이지만 조인다(승률 65%).
     * 1.020이면 습격대가 승수 1을 실제로 가져가고(15 대 16) 그 대가로 판당 타워 8.9기·
     * 골드 6,571을 청구한다 — 이게 이 기능이 팔려는 압박의 실제 크기다.
     *
     * ── 11단계: 난이도를 올리면서도 **여기는 손대지 않았다** (근거를 남긴다) ──────
     * 이번 작업의 목표는 "잘 두는 사람에게 너무 쉽다"였고, 사전 조사는 hpGrowth를
     * 유일한 손잡이로 지목하며 1.028을 권했다. **실측이 그 지목을 뒤집었다.**
     * hpGrowth가 실제로 사는 것은 **중반 보스(spino)의 누수**인데, 그건 최강 봇이
     * 원래 한 대도 안 맞는 축이고 초심자만 맞는 축이다. 판당 누수 피해(종별) 실측:
     *    hpGrowth 1.020 → 기준선 spino 7.0 / 최강 spino **0.0**
     *    hpGrowth 1.026 → 기준선 spino 9.5 / 최강 spino **0.3**
     *    hpGrowth 1.030 → 기준선 spino 13.8 / 최강 spino **1.6**
     * 승수로 보면(시드 20/40, **w50을 고치기 전 상태에서 잰 스윕**이라 여유의 출발점이
     * 82%다): 1.020 기준선 14 · 최강 여유 82% → 1.026 기준선 **7** · 최강 여유 59% →
     * 1.030 기준선 **4** · 최강 여유 54%. 곧 hpGrowth로 여유 55%에 닿으려면 초심자
     * 완주가 4/20까지 내려간다.
     * 곧 여유 −23%p를 사려고 초심자 완주를 절반으로 깎는 거래다. 같은 −22%p를
     * **초심자 비용 0**으로 사는 자리가 바로 아래 w50 trex의 HP였고, 그쪽을 썼다.
     * (봉투 1번 하한 팔이 13/20을 요구하므로 1.024 이상은 애초에 통과할 수도 없다)
     */
    hpGrowth: 1.020,
    seed: 1013,
    /**
     * 습격대 최소 구성(투창병 + 궁수)이 여기서 데뷔한다.
     *
     * 왜 초심자 스테이지에 넣는가: 스테이지1은 봇 기준 유일하게 완주 가능한 스테이지이고
     * (autoplay 실측), 실제로 플레이어가 끝까지 보는 유일한 판이다. 여기에 없으면
     * 부족 습격이라는 기능 자체가 사실상 화면에 등장하지 않는다.
     * 온보딩은 wavegen이 지킨다 — 웨이브 1~2는 swarm 고정이고 raid 편성은 웨이브 8부터다.
     *
     * 왜 투창병 **혼자가 아니라** 궁수와 한 쌍인가: 이 짝은 습격대가 근접이던 시절
     * (칼 1.5)에 "경로에서 두 칸 떨어뜨리면 영구 무력화"를 깨려고 만든 것이다
     * (실측: 스테이지2의 칼잡이 108명이 타워에 입힌 피해가 0). 9단계에서 전원이
     * 원거리가 된 뒤에도 짝은 그대로 유효하다 — 이제 축이 바뀌었을 뿐이다:
     * 투창병 2.4는 두 칸(2.0)에 닿고 세 칸(3.0)에는 못 닿으며, 궁수 3.2는 세 칸에도
     * 닿는다. 즉 "한 칸 물리면 투창병은 사라지고 궁수는 남는다"가 이 스테이지가
     * 가르치는 것이고, 그래서 해답이 배치 하나가 아니라 **배치 + 대응**이 된다.
     */
    allowedEnemies: ['raptor', 'compy', 'boar', 'trike', 'blade', 'archer'],
    bossOverrides: {
      10: bossWave(g('spino', 1, 0, 30, 0.6), g('raptor', 6, 20, 150)),
      20: bossWave(g('spino', 1, 0, 30, 0.85), g('trike', 3, 60, 180), g('boar', 6, 18, 90)),
      /*
       * ── 11단계: **w30·w40은 일부러 그대로 뒀다** (스윕 결과를 근거로 남긴다) ──
       * 착수 계획은 "보스가 봉우리가 아니다"(HP비 6.73 → 5.91 → 2.62 → 1.59 → 1.11의 단조
       * 붕괴)를 근거로 w30·w40 spino를 ×1.25~1.35 하라는 것이었다. **실측이 막았다.**
       *
       * (1) **잘 두는 쪽에는 값이 0이다.** spino 상대 hpMul을 1.1/1.2 → 1.25/1.35로 올려도,
       *     심지어 2.8/3.0(요격 불가 수준)으로 올려도 최강 봇의 여유는 52.0%로 **한 자리도
       *     안 움직인다**. 최강 봇은 이 두 웨이브에서 원래 기지 피해가 0이기 때문이다
       *     (판당 누수 피해 spino 0.0). 곧 이 손잡이가 파는 것은 사용자가 말한
       *     "잘 두는데 쉽다"의 해답이 아니다.
       * (2) **초심자에게만 비선형으로 비싸다.** 1.25/1.35에서 기준선은 13~14/20으로
       *     버티지만 1.40/1.50에서 10/20 · minT 6(죽음의 나선 하한 7 위반),
       *     2.8/3.0에서는 1/20이다.
       * (3) **골드 배분 봉투(7번)를 정면으로 민다.** 이 두 웨이브의 spino를 두껍게 하면
       *     **유닛 갈래만** 골라서 죽는다 — 아군은 마을 앞에서 붙잡는 자원인데, 붙잡아도
       *     못 죽이는 몸이 늘면 그 골드가 통째로 헛돈이 되기 때문이다. 실측(시드 20):
       *       1.1/1.2  → 타워 14/20 · **유닛 15/20**
       *       1.25/1.35 → 타워 13/20 · **유닛 10/20**  (타워는 −1인데 유닛은 −5)
       *     지금 wavegen에서는 하한(기준선 −3)에 간신히 걸치지만, 습격대 보장을 한 칸만
       *     촘촘하게 해도(주기 6 → 4) 그대로 깨진다. 봉투를 통과시키려고 1.15 같은
       *     중간값을 고르는 것은 **시드 20개짜리 잡음에 상수를 맞추는 짓**이라
       *     (4단계가 금지한 형태) 하지 않았다.
       *
       * 그래서 클라이맥스는 **w50 한 곳에서만** 복원한다(아래). HP비는
       * 6.73/5.91/2.62/1.59/**1.99**가 되어 마지막이 다시 가장 큰 봉우리가 된다.
       */
      30: bossWave(g('spino', 1, 0, 30, 1.1), g('trike', 3, 50, 150), g('boar', 6, 16, 240)),
      40: bossWave(
        g('spino', 2, 240, 30, 1.2),
        g('boar', 8, 15, 120),
        g('raptor', 8, 12, 330),
      ),
      /*
       * ── w50 클라이맥스 — **거짓 주석을 지우고 실제로 봉우리로 만들었다** ────────
       * 여기 적혀 있던 "w49 대비 총 HP ≥1.15×"는 **거짓이었다**. 실측 1.1073이고,
       * 커밋된 가드(tests/data/wavegen.test.ts, > 1.1)를 여유 0.0073으로 통과 중이었다.
       * 지금은 **1.993**이고, 같은 파일이 스테이지1 w50에 대해 ≥1.5를 따로 잠근다.
       *
       * 무엇을 바꿨나 — **trex의 HP**와 **몸 수**다.
       *  · trex hpMul 1.38 → **2.4**: 이 스테이지에서 여유를 살 수 있는 유일한 손잡이다.
       *    실측(최강 봇 40시드): 1.38에서는 trex가 18/40판만 통과했는데 1.9 이상에서는
       *    **40/40판 통과**다. 그 한 칸이 최강 봇의 여유를 82% → 60%로 내린다.
       *    그리고 **기준선 봇에게는 값이 0이다** — 기준선은 1.38에서도 이미 살아서 닿은
       *    판 전부(14/20)에서 이 trex를 통과시키고 있었다(승수·여유·minT 전부 무변).
       *    이전 판정 "w50 보스 HP는 무효 파라미터"는 **기준선 봇에서만 참**이었다.
       *  · 몸 수 17 → 29 (raptor 16→20, boar 8 추가): w49(56마리)와 밀도가 이어져
       *    "가장 센 웨이브가 가장 한산한 웨이브"이던 상태가 풀린다.
       *  · 2마리 trex(초안)는 채택하지 않았다 — 최강 여유는 65%로 오히려 덜 내려가면서
       *    (약한 개체 둘은 요격된다) 기준선만 14/20 → **4/20**으로 죽인다.
       * 남은 −8%p는 trex의 baseDamage로 샀다 (src/data/enemies.ts trex 주석에 표와 근거).
       */
      50: bossWave(g('trex', 1, 0, 30, 2.4), g('raptor', 20, 11, 210, 1.15), g('boar', 8, 14, 330)),
    },
  },
  firstClearAmber: 120,
  perWaveAmber: 1,
  unlockTowers: ['lightning'],
};
