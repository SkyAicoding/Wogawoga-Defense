/**
 * 스테이지 6 — 화산 (volcano). 최종장: 좌우 대칭 이중 경로가 z10에서 합류, 용암(~)이
 * 중앙 상단과 하단 코너를 차지. 화산 골렘(고장갑) 등장 — 독/체인 없이는 뚫기 어렵다.
 * 별 업그레이드 없이는 어려운 난이도 (hpBase 2.2, budgetBase 45).
 */
import type { StageDef } from '../types';
import { bossWave, g, v } from './helpers';

export const stage06: StageDef = {
  id: 6,
  nameKey: 'stage.6.name',
  biome: 'volcano',
  gridW: 13,
  gridH: 17,
  layout: [
    '.....~~~.....',
    '.o...~~~.o...',
    '.....~~~.....',
    '...o.~~~.....',
    '.............',
    '.o...o.o...o.',
    '.............',
    '.............',
    '#...........#',
    '...o.....o...',
    '.............',
    '.....o.o.....',
    '~...........~',
    '~......o....~',
    '~~.........~~',
    '~~.........~~',
    '~~....#....~~',
  ],
  paths: [
    [v(0, 2), v(4, 2), v(4, 6), v(2, 6), v(2, 10), v(6, 10), v(6, 15)],
    [v(12, 2), v(8, 2), v(8, 6), v(10, 6), v(10, 10), v(6, 10), v(6, 15)],
  ],
  /*
   * ── 하늘길 — **좌우 한 쌍, 남쪽에서 화덕을 친다** ───────────────────────────
   * ⚠ **s6 은 레인이 반드시 둘 이상이어야 한다** — w20 보스 오버라이드가 익룡에게
   *   `pathIndex 1` 을 준다(아래 g('ptera', 8, 18, 210, 1, 1)). 하나만 넣으면
   *   tests/data/stages.test.ts:206 의 pathIndex 범위 검사가 그 자리에서 빨개진다.
   * 지상 두 갈래는 (0,2)·(12,2) 로 **북쪽 어깨**에서 들어와 (6,10)에서 합류한다.
   * 하늘은 같은 좌우 변의 **남쪽**(z=13)에서 들어와 화덕을 아래에서 친다 — 접근 벡터가 다르다.
   * 각 길이 8.00 = 지상(23.00)의 0.348. 채점 0.840 동점 1위.
   */
  airPaths: [
    [v(0, 13), v(6, 13), v(6, 15)],
    [v(12, 13), v(6, 13), v(6, 15)],
  ],
  baseCell: v(6, 15),
  baseHp: 30,
  startGold: 300,
  waveCount: 50,
  wavePlan: {
    budgetBase: 45,
    budgetGrowth: 1.14,
    hpBase: 2.2,
    hpGrowth: 1.06,
    seed: 6091,
    allowedEnemies: [
      'raptor', 'compy', 'boar', 'trike', 'ptera', 'warrior', 'shaman', 'ankylo', 'mammoth', 'golem',
      'blade', 'lancer', 'archer', 'hexer',
    ],
    /*
     * ── 공중 축 게이트 (2라운드 2-a) ────────────────────────────────────────
     * 이 셋이 없을 때 실제로 벌어지던 일(실측, wavegen 을 직접 돌려 w1~50 을 뽑았다):
     *   · `isAirWave` 가 첫 조건에서 무조건 false → **보장된 하늘길 웨이브가 0개**
     *   · `allowed` 가 매 웨이브 전체 풀 → `genMixed` 가 아무 웨이브에서나 익룡을 뽑는다
     *     (s5 는 w4, s6 은 w3 부터 나왔다 — 둘 다 air_raid 가 아니라 mixed 였다)
     *   · `airLeft` 가 Infinity → 한 그룹이 GROUP_MAX_COUNT(25)까지 간다
     *     (s2 w34/42/46 · s3 w33/37/38/44 · s5 w29~47 이 **익룡 25마리** 웨이브였다)
     * 곧 "공중이 없다"가 아니라 **통제되지 않은 채 너무 일찍·너무 많이** 있었다.
     *
     * 값의 근거: 스테이지가 뒤로 갈수록 하늘이 **일찍 열리고 무리가 커진다**
     * (s1 22/2 → s2 18/3 → s3 16/3 → s4 14/4 → s5 12/4 → s6 10/5).
     * ⚠ **이 두 숫자는 밸런스 손잡이다** — 사용자가 돌린다(CLAUDE.md「밸런스는 …」).
     *   `curvePool: 'all'` 이 있는 한 이 값을 어떻게 돌려도 **HP 곡선은 안 움직인다**
     *   (게이트 유무만 곡선을 움직이고 값은 무관하다는 것이 실측이다).
     */
    /*
     * ⚠ **s6 은 게이트(`airFromWave`)를 걸지 않고 마릿수 상한만 건다.** 이유는 실측이다.
     *
     * 게이트를 걸면 w1~게이트 구간에서 익룡이 추첨 풀에서 빠지고, 그 예산이 지상으로
     * 재분배되면서 **초반 웨이브 구성이 바뀐다**. 기준선 봇은 s5·s6 에서 6웨이브에
     * 죽으므로 익룡을 한 마리도 못 보는데, 바로 그 초반 구성 변화 때문에
     * `tests/sim/ladder.test.ts` 의 판당 골드 단계(≤0.95)가 깨진다:
     *   s6/s5 골드비 — 배포본 0.8631 → 게이트 후 **0.9514**
     *   시드 집합 다섯(1000·2000·3000·4000·5000)에서 배포본은 0.8631~0.9163 로 전부 통과,
     *   게이트 후는 0.9514~0.9912 로 **전부 실패** — 잡음이 아니라 계통적 +0.08 이동이다.
     * 그리고 `airFromWave` 값으로는 못 고친다(8·10·14·18·22 전부 같은 값이 나온다 —
     * 봇이 그 웨이브에 닿지 못하기 때문이다). s6 예산을 올리는 것도 **계단이라 안 듣는다**
     * (×1.03 → 0.9666 · ×1.10 → 0.9671 로 오히려 나빠지고 ×1.20 에서야 0.7674 로 떨어진다).
     *
     * 상한만 걸면 다섯 시드 전부 최악 0.8589~0.9188 로 통과하고, 정작 병리적이던 것
     * — `airLeft` 가 Infinity 라 한 그룹이 `GROUP_MAX_COUNT`(25)까지 가던 것 — 은 사라진다.
     * (s5 는 w29~w47 이 전부 익룡 25마리 웨이브였다. 상한 뒤 한 웨이브 최대 8마리)
     *
     * ⚠ 남은 문제: **첫 익룡이 여전히 w3 로 이르다**(s1 은 w22). 그것을 늦추는 것은
     *   위 실측대로 사다리를 건드리므로 **밸런스 결정이라 사용자 몫으로 남긴다**
     *   (CLAUDE.md「밸런스는 사용자가 직접 한다」). 켜려면 아래 한 줄을 살리면 되고,
     *   `curvePool: 'all'` 이 이미 있어 HP 곡선은 그때도 안 움직인다.
     *   // airFromWave: 10,
     */
    airMaxCount: 5,
    // 곡선은 지금 그대로 못 박는다 — 근거는 types.ts 의 curvePool 주석.
    curvePool: 'all',
    bossOverrides: {
      10: bossWave(
        g('spino', 1, 0, 30, 1.0, 0),
        g('golem', 2, 90, 120, 1, 1),
        g('shaman', 3, 45, 180, 1, 0),
      ),
      20: bossWave(
        g('spino', 1, 0, 30, 1.0, 0),
        g('spino', 1, 0, 90, 1.0, 1),
        g('golem', 3, 75, 150, 1, 0),
        g('ptera', 8, 18, 210, 1, 1),
      ),
      // 클라이맥스 보정: 직전 웨이브 대비 총 HP ≥1.15×
      30: bossWave(
        g('trex', 1, 0, 30, 1.32, 0),
        g('golem', 5, 70, 150, 1.2, 1),
        g('shaman', 4, 45, 240, 1.2, 0),
      ),
      40: bossWave(
        g('trex', 1, 0, 30, 1.68, 1),
        g('spino', 1, 0, 300, 1.2, 0),
        g('mammoth', 4, 90, 150, 1.2, 0),
      ),
      50: bossWave(
        g('trex', 2, 420, 30, 1.1, 0),
        g('spino', 1, 0, 240, 1.0, 1),
        g('golem', 4, 70, 360, 1, 1),
        g('shaman', 4, 45, 480, 1, 0),
      ),
    },
  },
  firstClearAmber: 400,
  perWaveAmber: 3,
  // 2-c — 저장소 최초의 stun 타워. 마지막 판의 보상이라 축 하나가 아니라 제어 전반을 연다.
  //        ⚠ 보스는 stun 지속 1/5 + 종료 후 면역이라 이 타워로 보스를 묶을 수는 없다.
  unlockTowers: ['shockstake'],
};
