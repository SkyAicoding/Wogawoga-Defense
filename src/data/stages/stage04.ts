/**
 * 스테이지 4 — 설원 (snow). 첫 이중 갈래: 좌/우 스폰이 중앙 z7에서 합류해 하단 화덕으로.
 * 합류점 이후 공용 구간이 핵심 방어선. 매머드 등장, 얼어붙은 호수(~)가 하단 배치를 제한.
 */
import type { StageDef } from '../types';
import { bossWave, g, v } from './helpers';

export const stage04: StageDef = {
  id: 4,
  nameKey: 'stage.4.name',
  biome: 'snow',
  gridW: 12,
  gridH: 16,
  layout: [
    '............',
    '............',
    '.o.......o..',
    '....o.......',
    '.......o....',
    '..o.........',
    '.........o..',
    '............',
    '....o.......',
    '......o.....',
    '............',
    '....o.....~~',
    '~~....o...~~',
    '~~........~~',
    '~~........~~',
    '~~..#.....~~',
  ],
  paths: [
    [v(0, 1), v(3, 1), v(3, 7), v(5, 7), v(5, 14)],
    [v(11, 1), v(8, 1), v(8, 7), v(5, 7), v(5, 14)],
  ],
  /*
   * ── 하늘길 — **지상 두 갈래와 짝을 이루는 좌우 한 쌍** ──────────────────────
   * 지상이 (0,1)·(11,1) 두 갈래로 들어와 (5,7)에서 합류하므로 하늘도 좌우 한 쌍으로 둔다
   * (보스 오버라이드가 pathIndex 를 쓰는 판이라 레인 수를 지상과 맞춘다).
   * 동: (11,11)→(5,11)→화덕 길이 9.00 · 서: (0,11)→(5,11)→화덕 길이 8.00.
   * 지상(18.00)의 0.500 / 0.444. 채점 0.861 / 0.803 — 각각 1위와 그 거울.
   * ⚠ 북쪽 진입 짝(v(1,0)·v(9,0))은 길이 18.00 이라 **'지상보다 짧다' 계약을 못 넘어**
   *   탈락했다(tests/data/stages.test.ts:142). 계약이 실제로 후보를 걸렀다.
   */
  airPaths: [
    [v(11, 11), v(5, 11), v(5, 14)],
    [v(0, 11), v(5, 11), v(5, 14)],
  ],
  baseCell: v(5, 14),
  baseHp: 25,
  startGold: 260,
  waveCount: 50,
  wavePlan: {
    budgetBase: 33,
    budgetGrowth: 1.13,
    hpBase: 1.65,
    hpGrowth: 1.052,
    seed: 4057,
    // 습격대 4종 전원 — 스테이지3에서 완성된 해금은 이후 스테이지에서 유지된다.
    // (hexer 누락 수정: 3에서 나온 종이 4에서 사라졌다가 5에서 돌아오는 역행이었고,
    //  그 탓에 이 스테이지의 습격대 비율이 11.3%로 다른 스테이지의 절반 이하였다)
    allowedEnemies: [
      'raptor', 'compy', 'boar', 'trike', 'ptera', 'warrior', 'shaman', 'ankylo', 'mammoth',
      'blade', 'lancer', 'archer', 'hexer',
    ],
    /*
     * ── 공중 축 게이트 (2라운드 2-a) ────────────────────────────────────────
     * 이 셋이 없을 때 실제로 벌어지던 일(실측, wavegen 을 직접 돌려 w1~50 을 뽑았다):
     *   · `isAirWave` 가 첫 조건에서 무조건 false → **보장된 하늘길 웨이브가 0개**
     *   · `allowed` 가 매 웨이브 전체 풀 → `genMixed` 가 아무 웨이브에서나 익룡을 뽑는다
     *     (s5 는 w4, s6 은 w3 부터 나왔다 — 둘 다 air_raid 가 아니라 mixed 였다)
     *   · `airLeft` 가 Infinity → 한 그룹이 GROUP_MAX_COUNT(25)까지 간다
     *     (s2 w34/42/46 · s3 w33/37/38/44 · s5 w29~47 이 **익룡 25마리** 웨이브였다)
     * 곧 "공중이 없다"가 아니라 **통제되지 않은 채 너무 일찍·너무 많이** 있었다.
     *
     * 값의 근거: 스테이지가 뒤로 갈수록 하늘이 **일찍 열리고 무리가 커진다**
     * (s1 22/2 → s2 18/3 → s3 16/3 → s4 14/4 → s5 12/4 → s6 10/5).
     * ⚠ **이 두 숫자는 밸런스 손잡이다** — 사용자가 돌린다(CLAUDE.md「밸런스는 …」).
     *   `curvePool: 'all'` 이 있는 한 이 값을 어떻게 돌려도 **HP 곡선은 안 움직인다**
     *   (게이트 유무만 곡선을 움직이고 값은 무관하다는 것이 실측이다).
     */
    airFromWave: 14,
    airMaxCount: 4,
    // 곡선은 지금 그대로 못 박는다 — 근거는 types.ts 의 curvePool 주석.
    curvePool: 'all',
    bossOverrides: {
      // w10 첫 보스 벽 완화: 전체 ×0.6 (w9 대비 총 HP ~6.9배 → ~4.2배)
      10: bossWave(
        g('spino', 1, 0, 30, 0.51, 0),
        g('mammoth', 1, 0, 90, 0.6, 1),
        g('warrior', 5, 25, 150, 0.6, 0),
      ),
      20: bossWave(
        g('spino', 1, 0, 30, 1.1, 1),
        g('mammoth', 2, 90, 120, 1, 0),
        g('ptera', 6, 20, 180, 1, 0),
      ),
      30: bossWave(
        g('spino', 1, 0, 30, 1.05, 0),
        g('spino', 1, 0, 90, 1.05, 1),
        g('shaman', 3, 45, 150, 1, 0),
        g('trike', 5, 45, 210, 1, 1),
      ),
      // 클라이맥스 보정: 직전 웨이브 대비 총 HP ≥1.15×
      40: bossWave(
        g('spino', 2, 240, 30, 2.0, 0),
        g('mammoth', 3, 90, 150, 1.25, 1),
        g('shaman', 3, 45, 300, 1.25, 0),
      ),
      50: bossWave(
        g('trex', 1, 0, 30, 1.62, 0),
        g('spino', 1, 0, 420, 1.32, 1),
        g('ptera', 8, 18, 240, 1.2, 0),
      ),
    },
  },
  firstClearAmber: 250,
  perWaveAmber: 2,
  // 2-c — 주술사(shaman)가 s3 부터 나오므로 그 답인 주술 방해 토템을 **바로 다음 판**에서 연다.
  unlockTowers: ['hushtotem'],
};
